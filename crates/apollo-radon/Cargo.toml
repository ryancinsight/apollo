[package]
name = "apollo-radon"
version = "0.10.0"
rust-version.workspace = true
edition = "2021"
license = "MIT"
authors = ["Ryan Clanton <ryanclanton@outlook.com>"]
repository.workspace = true
description = "Discrete Radon transform and filtered backprojection plans for Apollo"


[features]
wgpu = [
    "dep:hephaestus-core",
    "dep:hephaestus-wgpu",
    "dep:bytemuck"
]
default = ["parallel", "mnemosyne-memory"]
parallel = []
mnemosyne-memory = []

[dependencies]
hephaestus-core = { workspace = true, optional = true }
hephaestus-wgpu = { workspace = true, optional = true }
bytemuck = { workspace = true, optional = true }
apollo-fft = { version = "0.25.0", path = "../apollo-fft" }
apollo-leto-interop = { version = "0.17.0", path = "../apollo-leto-interop" }
hermes-simd = { workspace = true }
leto = { workspace = true }
eunomia = { workspace = true, features = ["serde"] }
mnemosyne = { workspace = true }
moirai = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest.workspace = true
apollo-bench = { path = "../apollo-bench" }

[lints]
workspace = true

[[bench]]
name = "radon_wgpu_bench"
harness = false
required-features = ["wgpu"]
