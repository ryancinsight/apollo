# Apollo Backlog

<a id="apollo-workspace-instrumented-verification"></a>
## APOLLO-WORKSPACE-INSTRUMENTED-VERIFICATION — Close instrumented workspace coverage [patch] — blocked
- **Scope:** borrowed FourStep workspace boundaries and existing SIMD kernels; [ADR 0048](docs/adr/0048-worker-scratch-lifetime.md).
- **Evidence:** nightly Miri reaches the unchanged 4096/131072-element rejection test but exceeds 60 seconds; Windows ASan linking fails on missing `clang_rt.asan_dynamic_runtime_thunk-x86_64.lib`. Native analytical and allocation tests pass.
- **Acceptance:** unchanged boundary workload completes under a justified committed instrumented budget and supported unsafe execution paths pass a sanitizer; no workload reduction or diagnostic suppression.
- **Blocker / re-open:** x64 ASan runtime is absent from installed MSVC/LLVM; re-open when that runtime is available. Miri runtime attribution remains executable independently.
- **Verification:** attribute interpreter cost before modifying production code or its instrument; run targeted FourStep, lane and worker-idle tests with ASan once linking is available.

<a id="atlas-apollo-n32-f64-liveness"></a>
## ATLAS-APOLLO-N32-F64-LIVENESS-2026-09-04 — Bound the n=32 codelet and reduce register pressure [patch] [perf] — todo
- **Re-measured 2026-09-15 on both cores and both scalars, and the gap is one cell.** The probe bound the first processor class and `f64` alone, so no verdict here had ever read the efficiency core or `f32`; it now iterates the selection like its sibling probes. Over two runs per revision, `n = 32` `f64` is the only arm behind RustFFT — 1.057 and 1.074 of it on the efficiency core, whose paired runs spread under 1.5% — while `n = 16` reads 0.90, `n = 64` 0.94, and every `f32` arm 0.77 to 0.97. The performance core is too noisy to read here (identical revisions gave `f32`/64 1.034 and 0.939), so the efficiency core is the instrument. Evidence: `output/apollo-base128/small_codelet_liveness_2026-09-15.md`.
- **The measured cause was a host probe inside the dispatched frame.** `ComplexReg::zero()` reaches `Vector::try_zero`, which calls `runtime_support_result` — an `is_x86_feature_detected!` probe — and expects it, so each site emits a cached-feature load, two tests, a branch and a cold panic block even though the dispatcher entered under `#[target_feature(enable = "avx2")]`. The 32-point leaf carried eight, from the phase-two column array it initialized from that zero and then overwrote. Building the columns from the transposes directly takes the emitted body from 389 to 280 instructions with no probe left, and the cell from 1.057/1.074 to 1.044/1.042, controls unchanged. Figures are `scripts/codegen_attribution.py compare` over assembly emitted per revision from a clean export and copied out of the shared target directory before the next emit overwrote it; an earlier pass in the same session reported 324 from an ad-hoc extraction that stopped at the first return and so dropped the cold probe blocks, and read one count off an artifact hours older than either build.
- **What remains, and the next lead.** The cell is still 1.04: `n = 32` spends 8.75 instructions a sample against `n = 64`'s 3.84, because both leaves pay the same per-register load, twiddle and store overhead over half the data, and both round-trip a `mid` array between their phases (46 stack moves at `n = 32`). The next slice targets that round trip, and carries the two remaining probe sites in the 64-point leaf with a form that keeps its loop — removing that loop inflates it from 246 to 395 instructions, the same figure to the instruction whether the eight columns are built by `array::from_fn` or by eight explicit statements over constant indices, with stack moves 22 to 47 (both measured 2026-09-15, so the form is not the variable). Eight live registers do not fit beside the twiddles on AVX2, so the loop's stack array is cheaper there than its two probe sites; sourcing that leaf's zero from the capability token while keeping the loop is the untried form, and removing the loop is falsified.
- **The per-process mode is 4K aliasing, 2026-09-17.** `pinned_probe/page_offset.rs` times the n=32 f64 forward with its 64-byte-aligned samples at every 64-byte offset of a page, in one pinned efficiency-core process: 8 to 16 of the 64 offsets read up to 3.7% slower (44.3k against 42.7k ps), and in ten runs across both arms that band sits 2520 to 3050 bytes above the probe frame's stack address, that is 1046 to 1576 below it, where the transform's frames save and spill registers. Staging the samples on the stack inside the leaf leaves the band in place (the loads of the caller's samples follow the prologue's stores either way), so the mode is placed by the caller's buffer relative to the call frames, not by the leaf's code, and the cell's 1.04 is its lower mode. Evidence: `output/apollo-base128/n32_page_offset_2026-09-17.md`.
- **Registers held across the phases, 2026-09-17: not adopted.** Keeping phase one's two column pairs as registers removes the `mid` round trip (body 280 to 254 instructions, stack moves 46 to 28, no calls), but the pinned efficiency-core cell reads 1.042/1.038/1.072/1.041 on main against 1.062/1.068/1.041/1.039 changed, over four alternating rounds; controls unchanged. Both arms are bimodal across processes (about 43.0k and 43.9k ps for the same code), so the cell's 1.04 is the lower mode and the split, not the instruction count, is the next lead: attribute what selects the mode per process (data or stack alignment, the binding, frequency state) before another schedule change. Evidence and patch: `output/apollo-base128/n32_registers_held_2026-09-17.md`.
- **Scope:** n=32 AVX2 codelet, its caller and regression tests; shared radix-8 changes require n=64 controls. No provider migration or public API change.
- **Acceptance:** fixed-size safe entry; all direction/normalization and guarded-span tests pass; retain a schedule change only with counterbalanced same-host improvement and unchanged n=16/64 controls.
- **Safety increment verified:** array borrow establishes 32 initialized values; guarded tests cover both AVX alignment residues, assert host dispatch and reject NaN output. Release run `75c7d45c-4e69-4023-bbf7-30840d0020d6` passes 526/526 enabled tests (29 ignored instruments); format, all-target Clippy, doctest, rustdoc and safety ratchet pass. All three emitted AVX bodies match the entry baseline after label normalization.
- **Retained schedule:** 64-double stack intermediate, radix-4 stages then two radix-8 halves. Prior experiments are archived in `0a3a7a8f:backlog.md`; their cross-run absolute timings are not regression evidence.
- **Pair-emission experiment:** rejected for unestablished benefit, not proven slowdown. Nine focused and 526 enabled CPU library tests pass; the callback inlines and reduces forward/normalized frames 872→840 bytes.
- **Invalid timing evidence:** candidate/control runs exhibit unchanged-control drift and are discarded as optimization evidence. Raw samples remain diagnostic artifacts only; no speedup or slowdown is established.
- **Evidence:** immutable executable hashes, source/assembly snapshots and raw 100-sample CSVs under `../../output/fft-liveness/`, governed by Atlas output retention. Intel Core Ultra 9 285K, CPU 1 performance core, Rust 1.97.0 MSVC. No Miri/sanitizer or cross-target coverage is claimed.
- **Dispatch audit:** at `530ea774`, n=32 bypasses tiny dispatch (2/3/4), uses fixed plan executors and selects AVX/FMA through a cached capability predicate. No runtime autotuning, input-dependent route or plan twiddle-cache read occurs. Independent source review agrees; this rules out plan reselection with unchanged capabilities, not host or loader effects.
- **Instruction analysis:** `../../output/fft-liveness/scheduling-model.json` records all three historical baseline/candidate bodies under LLVM 22.1.8. Forward instructions 284→282, normalized inverse 302→299, inverse 282→283. `arrowlake-s` uses `AlderlakePModel`; changing the alias assumption reverses estimated cycle directions. These are static/model observations, not counters or performance acceptance.

<a id="atlas-apollo-strided-gather-upstream"></a>
## ATLAS-APOLLO-STRIDED-GATHER-UPSTREAM-2026-09-03 — The split's gather is hand-rolled per block count instead of a substrate primitive [minor] [arch] — todo
- **Re-bounded 2026-09-11 (slice 7 of [the two-pass base](#apollo-l1-base-two-pass)):** the four-lane width no longer gathers at all — each block reads its subsequence out of the parent — so the primitive this item asks for serves the eight-lane width only, where a strided block register costs up to four windows and three shuffles and the gather measured better. The acceptance's `GatherBlocks` deletion now means its eight-lane use; the eight-block split it names is deleted.
- **The shape of the problem.** Apollo's base-128 split reorders its input by a strided gather: subsequence `b` of stride `blocks` starting at the bit-reversed offset `rev(b)`. That is a multi-slice read — `data[j * blocks + offset]` for every `j` — and it is written twice: once as a SIMD blend network in `split_boundary::GatherBlocks`, hand-written for `BLOCKS = 2` and `BLOCKS = 4` and asserting on anything else, and once as a scalar strided loop as the fallback. Every new block count needs a third hand-written network, which is exactly what stopped the eight-block split (`#atlas-apollo-eight-block-split`).
- **It is worth a measured amount.** Disabling the four-block network at n = 512 costs f64 +7.5% and f32 +15.4%, so the network earns its place — the objection is to where it lives and that it is arity-bound, not to its existence.
- **The substrate already has the vocabulary, and apollo does not use it.** Leto carries the ndarray-shaped surface — `exact_chunks`, `axis_chunks_iter`, `windows`, `lanes` / `lanes_mut`, `axis_iter`, `task_partitions_mut` — and apollo's hot kernels index raw slices instead. Hermes carries interleaved-complex arithmetic (`interleaved_complex_mul_assign`, `interleaved_complex_dot`) but no lane-permutation primitive: the pair deinterleave apollo needs is defined inside apollo, so no other consumer can reach it and no other backend can implement it.
- **Where each piece belongs.** The lane-level deinterleave is a hermes capability parameterised by block count, not an apollo one: it is pure register permutation, it has a natural AVX2/AVX-512/NEON implementation per backend, and every mixed-radix consumer in the stack wants it. The block-major view of a strided array is a leto capability — a gather into `blocks` contiguous lanes is `lanes()` with a stride, and leto already models strided views. Hephaestus wants the same decomposition for its device kernels, where a badly shaped gather costs far more than it does on CPU. Filed upstream as `#hs-strided-block-gather`; this item is apollo's side of it — adopting the primitive and deleting the two hand-rolled copies.
- **Not a refactor for its own sake.** The measured claim is narrow: apollo cannot extend its split past four blocks without a third hand-written network, and the pass-count problem that makes the split worth having at all is a data-movement problem, which is the substrate's concern. Whether a generic primitive matches the hand-written arity-2 and arity-4 networks is itself a measurement — the acceptance below requires it, because a generic gather that costs 7.5% more than the hand-written one would be a regression wearing an architecture argument.
- **Acceptance.** One strided block gather, owned upstream, generic in block count, measuring no worse than the current hand-written networks at `BLOCKS = 2` and `4`; apollo's `GatherBlocks` and its scalar fallback both deleted; and the eight-block split re-measured against the flat route with it in place.

<a id="atlas-apollo-f32-nonpot-width"></a>
## ATLAS-APOLLO-F32-NONPOT-WIDTH-2026-09-03 — f32 loses far more than f64 on non-power-of-two lengths [patch] [perf] — todo
- **Re-measured 2026-09-15 (`codelet_selection` and `short_winograd_leaves`, bench-quick, pinned; `output/apollo-base128/codelet_selection_2026-09-15.txt`, `short_winograd_leaves_2026-09-15.txt`):** the leaf 11 no longer carries the excess — f32 0.36x of f64 on the performance core, 0.39x on the efficiency core — yet the codelets built on it still do: n = 121 (11 x 11) f32 1.81x of f64 and n = 154 (2 x 7 x 11) 1.53x on the performance core, 1.42x and 1.50x on the efficiency core, with their composite arms slow in the same proportion. The other retained f32 codelet lengths run below f64 (222 0.86x, 246 0.86x, 259 0.93x, 296 0.89x; 96 is f32-only). Leaves over 1.1x on the performance core: 12 (1.25x), 23 (1.33x), 24 (1.54x), 144 (1.21x); on the efficiency core: 3 (2.0x at 3 ns), 29 (2.82x — the wide pair form pays the efficiency core's two-micro-op 256-bit cost, see `#apollo-chain-pass-e-core-census`) and 144 (1.34x). Next locus: the twiddle stage between the leaves in `dft121_impl` and the 154 Good-Thomas codelet, since their leaves are fast; then the 12/24 and 23 leaves on the performance core and the wide 29 on the efficiency core.
- **Re-measured 2026-09-17, and the 121 codelet no longer lags.** On main, pinned, release: f32/f64 is 0.91 (P) and 0.94 (E) for the n = 121 codelet, 1.06 and 1.18 for the n = 154 codelet, 1.27 and 1.17 for the n = 121 composite route (`output/apollo-base128/pair_columns_2026-09-17.md`). Both routes reach radix 11 through the same scalar `dft_pair_impl` for both scalars (no scalar-dependent route at these lengths); on the composite route the f32 group loop is inlined into the AVX2 `FlatStockham` frame while f64's stays out of line at baseline, the next lead for the remaining composite gap. Rejected: interchanging `dft_pair_impl`'s loops (all H outputs of an accumulator advancing together, bit-identical) doubles the f32 11 leaf (11.5k to 22.9k ps on P) and slows every route that uses it.
- **Frame placement falsified, 2026-09-17.** Calling the composite route's scalar radix stage out of line (`#[inline(never)]`, compiled at baseline like f64's) left `f32` 121 composite level on the performance core (502 to 508k against 510 to 514k ps) and 2.5% under on the efficiency core, 154 level on both (`codelet_selection`, three alternating runs; `output/apollo-base128/composite_scalar_stage_2026-09-17/`). Where the f32 radix-11 stage compiles is not what makes composite 121 1.27x its f64; not landed.
- **Lease:** N=101 composition probe and `components/rader/convolution.rs`; no edits to the staged FourStep, codelet, or comparator work.
- **Acceptance for this increment:** same-run, queried-class pinned attribution separates plan/Rader dispatch, half-cyclic twiddle phases, and phase-memory traffic at N=101 f32; retain only a production change that improves N=101 and does not regress the matched prime/composite controls.
- **Composition attribution, 2026-09-06:** the retained ignored probe is source-hashed `ECE113F38F534B416876F4A1CFBE91B7D8CB6F068A1A609FCE28A450C65F2B37` and passed locked `bench-quick` Nextest run `49557df2-bee5-475c-b8dc-b759471676f1`. It binds queried CPU 1 (performance) and CPU 3 (efficiency), reports the real N=101 production composition plus local split/recombine twiddle controls, and includes N=97, 113 and 151 composition controls in the same run. N=101 composition medians were 751,120 ps (P) and 761,862 ps (E); split/recombine twiddle deltas were separately visible, with the full-slice barriers retained.
- **Decision:** static-radix and fixed-short-50 production candidates are rejected. Counterbalanced target runs made fixed-short-50 slower by about 6--7% on the performance core and mixed on the efficiency core; the static-radix route was slower on the target and controls. No production convolution change is retained. The shipped path remains the pre-probe implementation; this increment retains only the attribution instrument.
- **Finding.** On composite and prime lengths apollo's `f32` is much further behind RustFFT than its `f64` is, which inverts the expected order: AVX2 gives `f32` twice the lanes, so it should be *relatively* better. | n | apollo f64 vs RustFFT | apollo f32 vs RustFFT | | --- | --- | --- | | 180 | +20 to +43% | +52 to +67% | | 384 | +12 to +40% | +64 to +70% | | 101 | +14 to +17% | +91 to +95% |
- **The sharpest form of it:** at n = 101 apollo's `f32` measures 674 ns against its own `f64` at 579. A prime-length transform where the narrower scalar is slower is not a tuning gap; it means the f32 path is not running at its width.
- **Already excluded.** Both scalars route `composite_forward` to the same generic `radix_composite::forward_inplace_with_radices`, so it is not a different function. The Rader backend choice is scalar-parameterised (`prefers_half_cyclic_for_rader::<F>`) but both `f32` and `f64` declare `HALF_CYCLIC_RADER_THRESHOLD = 32`, so n = 101 takes the same backend on both. The primitive root is cached for p < 4096, so it is not per-call derivation.
- **Narrowed by the absolute numbers, which say this is two problems.** At the composite lengths `f32` *is* faster than `f64` — 313 ns against 499 at n = 384 (0.63x), 142 against 204 at n = 180 (0.70x) — so it is using its width there, and `radix_composite` does carry `f32` AVX2 passes for radix 2, 3, 4, 5 and 7. What it does not do is extract as much from the extra lanes as RustFFT: RustFFT's `f32` is 0.53x its own `f64` at n = 384 against apollo's 0.63x. That is a narrower claim than "not vectorised", and the composite half of this item is that ratio.
- **The prime half is the anomaly.** At n = 101 apollo's `f32` (674 ns) is slower than its own `f64` (579). No lane-width argument explains that, and it is the Rader path rather than the composite one. Look at the lane dispatch inside the Rader convolution for a capability query a scalar backend satisfies — `exact_lanes_supported::<4, T>` answering true through a scalar implementation is a known shape in this codebase and would leave `f32` running unvectorised while reading as supported.
- **Instrument: `pinned_probe/rader_width.rs`, and the anomaly does not reproduce at the Rader entry point.** Times `rader_prime_forward` for both scalars over eight primes chosen so the convolution length `p - 1` spans the radix mixes, both scalars inside one pinned run per core. - **Result (optimized, 2026-09-03).** `f32` is never meaningfully slower than `f64` on either core class. n = 101 measures 872 ns for `f64` against 804 ns for `f32` — ratio 0.92 — and the larger lengths run 0.76 to 0.87. The recorded 674-against-579 ns does not reproduce here. - **Scope.** This measures `rader_prime_forward`; the item's figures came from the full transform path. Different quantities, so this narrows the prime half to "not the Rader kernel itself" rather than closing it. The next place to look is the dispatch around it. - **Two measurement errors were made and corrected before this stood.** Apollo defines no `[profile.test]`, so `cargo nextest run` built the probe at opt-level 0 — timings of unoptimized code, which cannot answer a width question; the probe now refuses to report from a debug build. And the report's `median_ps` is *already* per-iteration (`normalized_picoseconds` divides by `iterations_per_sample`), so dividing by that column again is wrong: it manufactured an apparent 0.4 ns for a 17-point transform and, because the two arms calibrate to different iteration counts, injected a spurious factor into the ratio itself. An earlier revision of this item reported the resulting phantom as an unsound instrument, and one run of it as confirming the anomaly at 1.19. Neither was real. Any future reader of this probe: `median_ps` as printed is the number; do not normalize it again. - **The reset is not timed.** `BenchmarkSuite::run` measures the whole closure, so a per-iteration `copy_from_slice` was charged to the operation — and `Complex64` copies twice the bytes of `Complex32`, biasing the exact ratio being read. `apollo-bench` gained `run_batched`, which builds one input per iteration before the timer starts.
- **A third locus, measured 2026-09-03 by the codelet-selection sweep.** That sweep timed two routes over the same twenty lengths in one pinned run, once per scalar, so it compares `f32` against `f64` *within a route* rather than against a reference. The two routes answer oppositely: | route | median `f32`/`f64` | reading | | --- | --- | --- | | composite | **0.80x** | `f32` is using its extra lanes | | short-Winograd codelet | **1.28x** | `f32` is slower than `f64` at 17 of 20 | Worst cases on the codelet arm: n = 144 at 1.83x, 99 at 1.50x, 112 at 1.46x, 189 at 1.45x, 120 at 1.44x. This is the same shape as the n = 101 prime anomaly and it is not the Rader path, so whatever leaves `f32` running narrow is not confined to the Rader convolution.
- **Re-measured 2026-09-04, and the boundary moved.** `codelet_selection` was resetting its buffer inside the timed closure and building at opt-level 0. An earlier note here claimed the *within-scalar* comparison was unaffected "since both arms copy the same bytes" — that is wrong. A constant added to both arms does not bias either one, but it compresses their **ratio** toward 1, and the ratio is what the boundary is drawn on. Rebuilt with `run_batched` under `bench-quick`, two lengths crossed: - **f32 drops 242 and 363.** 363 loses on both core types (1.02x, disjoint intervals); 242 loses on the performance core (1.04x, [1412,1446] against [1369,1395]) and only ties on the efficiency core. - **f64 drops 484.** It loses on both core types, 1.15x and 1.34x — and it is divisible by 11, which falsifies the "codelet wins at every accepted length divisible by 11" rule that list stated as exact. - **f64 keeps 242 on mixed evidence**, recorded rather than resolved: 1.06x loss on the performance core against a 0.80x win on the efficiency core, with one compile-time decision covering both. - **The PFA lengths are no longer unmeasured.** 222, 246, 259 and 296 now compare against the coprime/PFA route they actually take, and are the largest wins in the sweep — 0.35x to 0.52x at f32, 0.48x to 0.69x at f64. The cross-scalar 1.28x figure below predates all of this and is not carried forward; the width question is now the dispatch, not the codelet arm.

<a id="atlas-apollo-beat-the-references"></a>
## ATLAS-APOLLO-BEAT-THE-REFERENCES-2026-09-03 — Apollo must be faster than RustFFT and PhastFT at every size [minor] [perf] — todo
- **Standing bar.** Neither RustFFT nor PhastFT may be faster than apollo at any length. This item is the scoreboard and the parent of the work; each gap below becomes its own increment.
- **Scoreboard 2026-09-18 (main `f480ecdc`, after #501 to #505; `small_sizes_against_the_references_by_core_type`, release, pinned, two runs, apollo over RustFFT, min to max; `../../output/apollo-scoreboard-2026-09-18/`).** At or above RustFFT: `f64` 32 1.04 to 1.08 (performance) and 1.03 to 1.05 (efficiency), 1000 0.93 to 1.01 / 1.00 to 1.01, 1024 0.98 to 1.01 on the efficiency core, 16 0.90 to 1.13 on the performance core (both arms spike in single runs); `f32` 8 1.00 to 1.04 (performance), 384 1.00 to 1.03 / 1.03 to 1.04, 1024 1.01 to 1.03 on the efficiency core. Closed since 2026-09-17: `f64` 8 (0.85 to 0.94), 180 (0.89 to 0.92), `f32` 16, 180, and the efficiency core's 8192 and 16384. Every other cell is below 1.0, among them `f64` 101 0.64 to 0.75, 384 0.65 to 0.99, 4096 0.74 to 0.93, `f32` 64 0.58 to 0.84. The open cells are `f64` 32, 1000, 1024 (efficiency) and `f32` 8, 384, 1024 (efficiency).
- **Scoreboard 2026-09-11 (aligned tables, the pinned probe, performance core, apollo over RustFFT / PhastFT):** `f64` 128 1.02, 256 1.00, 512 0.96, 1024 1.01 to 1.05, 2048 1.11 to 1.24, 4096 1.07 / 1.22, 32768 0.99 / 0.84, 100 0.73, 101 0.62 to 0.68, 180 1.47, 384 1.26, 1000 0.99, 1009 0.80; `f32` 128 0.99 to 1.03, 256 1.01, 512 0.97, 1024 1.05, 2048 1.29 / 1.02, 4096 1.20 / 1.07, 32768 1.06 / 0.81, 100 0.75, 101 0.67 to 0.70, 180 1.63, 384 1.37, 1000 0.85, 1009 0.86; the small lengths after the call path and the rotation ([#432](https://github.com/ryancinsight/apollo/pull/432), [#433](https://github.com/ryancinsight/apollo/pull/433), [#434](https://github.com/ryancinsight/apollo/pull/434)): `f32` 8 1.07, 16 0.99 to 1.01, 32 0.95, 64 0.67 to 0.71, `f64` 8 1.11 to 1.22, 16 0.90, 32 1.07, 64 0.97 to 1.00; 180 at `f32` on the column route ([ADR 0062](docs/adr/0062-composite-bases-as-register-kernels.md)) 0.94 to 1.03 from 1.47, the efficiency core 0.98 from 1.25, `f64` 180 on the same route 1.06 / 0.68 from 1.48 to 1.52 and 0.95 on the efficiency core from 1.27; and the base routes with hermes' sign-flip rotation: `f64` 128 0.77 to 0.82, 256 0.86, 512 0.84 to 0.85, `f32` 128 0.92 to 0.94, 256 0.88 to 0.90, 512 0.88 to 0.91 (`../../output/apollo-base128/small_sizes_kernelgap2_run*_2026-09-11.txt`). Delivered today: the single-pass 512 base at both widths ([#420](https://github.com/ryancinsight/apollo/pull/420), [#421](https://github.com/ryancinsight/apollo/pull/421)), the aligned tables ([#423](https://github.com/ryancinsight/apollo/pull/423)), the 128 shape by width ([#424](https://github.com/ryancinsight/apollo/pull/424)), 2048 as four 512-blocks ([#425](https://github.com/ryancinsight/apollo/pull/425)), the Rader backend selection ([#427](https://github.com/ryancinsight/apollo/pull/427)). Open: [the composite census](#apollo-composite-radix-census) (180, 384), the 2048 sink and 4096, `f64` 8.
- **Base-128 floor (2026-09-10).** At 512 and 1024 the eight-block construction is 1.22 and 1.29 times RustFFT (`f64`); its bare 128-point kernels alone take 65% of the RustFFT budget at 1024, and the gather, combine sinks and last level add 38%, of which the two filed levers ([gather in the loads](#apollo-base128-gather-in-loads), [level in the sink](#apollo-base128-level-in-sink)) can remove about half. Parity at these lengths needs a construction with fewer passes over the data (a two-stage 32 by 32 kernel), not a faster piece of this one. RustFFT's plan at these lengths (read 2026-09-11): 512 one butterfly, 1024 four 256-point butterflies under one radix-4 step, the 256 itself two passes; filed as [the two-pass base](#apollo-l1-base-two-pass).
- **Where it stands.** `small_sizes_against_the_references_by_core_type`, performance core, minimum of 100 pinned samples, hoisted plans on every side (RustFFT's planner and PhastFT's are hoisted exactly as apollo's is, so the comparison is like for like). Percentages are apollo against RustFFT; negative means apollo is ahead. PhastFT's DIT planner is power-of-two only and has no arm elsewhere. | n | f64 | f32 | class | | --- | --- | --- | --- | | 8 | +14% | +49% | pot | | 16 | +40% | +9% | pot | | 32 | +39% (see `#atlas-apollo-n32-f64-liveness`; the +105% here was load-inflated) | +25% | pot | | 64 | **-12%** | **-2%** | pot, tuned `State64` | | 128 | +8% | +25% | pot, tuned split | | 256 | +20% | +31% | pot, tuned split | | 512 | +23% | +32% | pot, tuned split | | 1024 | **+58%** | **+61%** | pot, untuned | | 4096 | +31% | +36% | pot, untuned | | 32768 | +36% | +42% | pot | | 100 | **-15%** | **-32%** | composite | | 180 | +38% | +49% | composite | | 384 | +35% | +37% | composite | | 1000 | +24% | **-9%** | composite | | 101 | +19% | **+95%** | prime | | 1009 | +18% | +28% | prime | Element-wise minimum of two runs, taken after `#305` narrowed the generated-codelet selection. The machine carries concurrent peer builds and the spread is large — n = 32 `f64` read +33% on one run and +105% on the next — so any increment must re-measure its own cells rather than trust this table's precision. What is stable across every run this session is the set of signs: apollo is ahead at n = 64, n = 100 and `f32` n = 1000, and behind everywhere else.
- **Large lengths, 2026-09-09.** The planar route (ADR 0053, 0056 to 0059) now covers 4096 through 2097152: in `f64` apollo is level with RustFFT at 4096 and 262144, ahead at 16384 and 1048576, 1.04 behind at 65536 and level at 2097152; in `f32` ahead of both references at every measured length except 2097152 (PhastFT by 10%). Evidence in the closed items above and `../../output/apollo-n1m-crossover/`.
- **Odd powers, 2026-09-09.** The rectangular route (ADR 0060) replaces the split: 2048 `f64` 1.24 behind RustFFT (was 1.55), `f32` 1.7 (was 1.9); 32768 `f32` 1.13 (was 1.25), `f64` level. The remaining odd-power gap is the seam sweeps' cost per element at 2048 (`t1` 1.8, `f2` 2.2 cycles against the plain sweeps' 0.6 to 1.0), the same cost the even route pays and the next lever for both. Evidence `../../output/apollo-planar-rectangular/`.
- **Standing, 2026-09-10, main after #379** (`../../output/apollo-scoreboard-2026-09-10/`, the same three instruments, two runs each). Unchanged within the run spread: 512 1.2 / 1.2, 1024 1.3 / 1.3, 2048 1.26 / 1.6, 4096 1.0 / 1.1, 16384 1.06 / 1.05, 32768 1.02 / 1.06, 65536 1.0 / 0.91, 262144 0.83 `f64` (`f64` / `f32`, below 1 ahead). The direct sink (#373) moves only callers whose rows sit on a line, which the benches' per-iteration clone is one process in four. The odd-power seams now sit at measured floors: the transpose at the register network's shuffle floor (ADR 0060, the staged and the load-folded forms both rejected), the sink at its interleave; the gap at 2048 `f32` is the pass count against RustFFT's three radix-16 passes in L1, not a seam, so the next lever there is a shorter route, not a cheaper seam.
- **Standing, 2026-09-09, main `52c84126`.** `engine_census` (cache flushed, `f64`), `rustfft_comparison` and `twiddless_comparison` (clone-inclusive, both precisions), pinned performance core, two runs each (`../../output/apollo-scoreboard-2026-09-09/`). Apollo against RustFFT, the two runs' medians; below 1 is ahead. | n | f64 | f32 | route | | --- | --- | --- | --- | | 512 | 1.2 | 1.0 | base-128 | | 1024 | 1.25 | 1.17 | base-128, level vs PhastFT | | 2048 | 1.27 | 1.6 | planar rectangle | | 4096 | 0.95 to 1.05 | 1.0 | planar square | | 16384 | 1.03 to 1.07 | 1.0 | planar square | | 32768 | 1.04 | 1.09 | planar rectangle | | 65536 | 1.03 | 0.9 | planar square | | 262144 | 0.9 | (see item) | planar square | | 640 | 1.3 | 1.4 | composite | | 5120 | 1.2 | 1.3 | composite | | 12288 | 1.3 | 1.7 | composite | | 67, 121 | 1.3 to 1.7 | 2 to 3 | prime, `#atlas-apollo-f32-nonpot-width` | The even planar lengths are level or ahead; what remains is the odd powers' seam cost at 2048 and 32768 (both sweeps at twice the plain sweeps' rate per element, largest in `f32`), the base-128 route below 2048 (gather and four-block chains), and the composite and prime `f32` routes under their own items. The September table below is superseded.
- **Alignment, 2026-09-09.** The planar route ran half again slower whenever its scratch landed part way into a cache line, three allocations in four; the planes now start on a line (item below), which is what the bimodal 2048 `f64` bench readings and the reference-arm drift in earlier rounds partly were. Re-measure the standing cells above once it lands.
- **1024, 2026-09-09.** The base-128 route's last level vectorized: `f64` 1.5 to 1.6 µs against RustFFT's 1.2 (was 1.8), `f32` 0.7 to 0.8 against 0.6 to 0.7 (was 0.8). What remains at 1024 is the gather and the two four-block chains (`split_pieces_by_size`: at 512 the gather is 19% and the combine 22% of the route).
- **What the shape says.** Apollo is ahead exactly where it has a hand-tuned construction — n = 64 (`State64`) and n = 100 — and behind everywhere else, including at lengths where the tuned split applies (128 to 512 sit at +8 to +36%). So this is not a handful of bad sizes: the tuned paths are competitive and the general paths are roughly 10 to 40% behind, with two untuned power-of-two lengths (1024, 4096) and the f32 non-power-of-two family much worse.
- **The f32 non-power-of-two family is the sharpest lead.** At n = 101 apollo's `f32` (674 ns) is *slower than its own `f64`* (579), which cannot be right when `f32` has twice the lanes. The same shape appears at 180 and 384, where `f64` sits at +12 to +20% and `f32` at +52 to +70%. Something in the f32 composite and prime paths is not using its width. Filed as `#atlas-apollo-f32-nonpot-width`.
- **Hypotheses already falsified**, so nobody re-runs them: (1) the f64 n = 32 arm loading sixteen YMM registers and spilling — forcing the Winograd fallback instead measured 33.4 ns against the vector arm's 31.0, so both paths are ~2x RustFFT and register pressure is not the cause; (2) n = 1024 being on `exec_pot_forward_sized::<10>` rather than the generic executor — the generic executor is worse (f32 1236 ns against 953); (3) the plan's call preamble (`assert_plan_length` plus `runtime_tiny_direct_dispatch`) — it costs 0.12 ns at n = 8 and nothing measurable above; (4) apollo's own composite route beating its small codelets — the codelet wins by 27 to 279% at n = 16 to 64.

<a id="apollo-audit-2026-09-18"></a>
## APOLLO-AUDIT-2026-09-18 — Memory and capability findings filed from the 2026-09-18 audits — todo
- **Basis:** main `f480ecdc`; evidence, acceptance oracles and scope per item in [../../output/apollo-memory-audit-2026-09-18.md](../../output/apollo-memory-audit-2026-09-18.md) and [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md). The two correctness findings are [the NUFFT scale](#apollo-nufft-3d-type2-scale) and [the NTT fields](#apollo-ntt-field-validation). Each line below is one item; claim it by anchor.

<a id="apollo-mem-3d-scratch-roles"></a>- **APOLLO-MEM-3D-SCRATCH-ROLES** — The 3-D C-order chain holds two full-volume scratch roles on the caller thread [patch] — todo. Measured 2.0x data retained after 64³ to 128³ (64 MiB at 128³ f64); one role serves. Acceptance: retained bytes 1.0x data under the audit's allocator probe, 3-D census level. [../../output/apollo-memory-audit-2026-09-18.md#f1]
<a id="apollo-mem-dead-stage-tables"></a>- **APOLLO-MEM-DEAD-STAGE-TABLES** — Base and chain routes leave the full stage-major table dead in the twiddle cache [patch] — done. The sink and chain tables are relaid from a transient table (`MixedRadixScalar::transient_twiddle_{fwd,inv}`, the cache's own builder without the insert), so the process no longer keeps 16n bytes per direction (f64) for its life; `base_route_plans_leave_no_stage_major_table_cached` covers f64 1024, 16384, 262144 and f32 4096 and fails on the cached form. Execution untouched.
<a id="apollo-mem-bluestein-per-call"></a>- **APOLLO-MEM-BLUESTEIN-PER-CALL** — Bluestein allocates 32(n+p) bytes and re-transforms its kernel every call [patch] [perf] — done. The plan owns `ChirpTables` per direction (inverse on first use) and borrows the padded buffer from the Bluestein scratch role: zero warm allocations (`bluestein_warm_execution_is_allocation_free`, fails on the per-call form), bitwise equal to the free route, and 2.0 to 2.4x faster on both cores at 361 to 2209 (`../../output/apollo-mem-bluestein-per-call-2026-09-18/`). The free-function route still builds its tables per call.
<a id="apollo-mem-inverse-conjugate"></a>- **APOLLO-MEM-INVERSE-CONJUGATE** — Inverse tables duplicate the forward ones as conjugates [patch] — in-progress (integrator Claude; slice 1, base-route sinks, delivered; slice 2, the batched four-step fold, delivered: one forward `FourStepFold` per length, the inverse pass negates the fold product's imaginary part, bitwise unchanged at every batched length in f64/f32, first inverse at 65536 f64 4,128 bytes from 282,744; slice 3, the four-step `W_N^{jk}` matrix, delivered: cached forward-only per shape, each consumer (generic four-step, batched interleaved, two-by-prime) conjugating at the multiply, bitwise unchanged, first inverse at 2^22 f64 33,300 bytes from 67,142,180; slice 4a, the radix-2 split's combine (odd powers from 2^23), delivered: forward stage table conjugated at the multiply, first inverse at 2^23 f64 134,217,728 bytes less, bitwise unchanged; slice 4b, the remaining Stockham stage-major tables, next: `inverse = conj ∘ forward ∘ conj` is bitwise for these FMA shapes, the conjugations to fuse into the first and last stages). Acceptance: inverse retained bytes halved with inverse timings inside the identical-code band. [../../output/apollo-memory-audit-2026-09-18.md#f3]
<a id="apollo-mem-cache-bounds"></a>- **APOLLO-MEM-CACHE-BOUNDS** — No plan or kernel cache is bounded, evictable or clearable [minor] [arch] — in-progress (integrator Claude; ADR 0068; slice 1, the plan caches bounded with `clear_plan_caches()`, delivered; slice 2, kernel tables owned by plans and held `Weak` by the caches with the `_RAW` fast path replaced under Miri, next). 2.3x data per distinct smooth length, 8.8 to 11.8x for Rader primes; eviction first needs the raw-pointer thread-local caches off their never-dies safety premise. Acceptance: a public clear with a test that retained bytes return to baseline, and a bound with a documented policy. [../../output/apollo-memory-audit-2026-09-18.md#f4]
<a id="apollo-mem-four-step-matrix"></a>- **APOLLO-MEM-FOUR-STEP-MATRIX** — The generic four-step keeps a full W_N^{jk} matrix above 2^21 [patch] — todo. 67 MB at 2^22 f64, one matrix since APOLLO-MEM-INVERSE-CONJUGATE slice 3 (was one per direction); the compact two-level fold (ADR 0059) is the candidate. Acceptance: table bytes measured down, 2^22 timing not slower. [../../output/apollo-memory-audit-2026-09-18.md#f5]
<a id="apollo-dead-interleaved-four-step"></a>- **APOLLO-DEAD-INTERLEAVED-FOUR-STEP** — `batched::interleaved::four_step_interleaved` has no production caller [patch] — todo. Reached only by its unit tests and `codelet/pinned_probe.rs`; route it where the probe measures it faster than the planar batched route, or delete it with its tests. Acceptance: either a dispatch arm with pinned evidence or the module removed; nextest green.
<a id="apollo-mem-odd-power-scratch"></a>- **APOLLO-MEM-ODD-POWER-SCRATCH** — Odd powers on the planar route keep 2x data of caller scratch [patch] — todo. Acceptance: scratch bytes measured at 524288 and 2097152, timings level. [../../output/apollo-memory-audit-2026-09-18.md#f6]
<a id="apollo-mem-chain-build-copies"></a>- **APOLLO-MEM-CHAIN-BUILD-COPIES** — Chain-level construction holds three copies of each level transiently [patch] — todo. Acceptance: peak bytes during plan build measured down. [../../output/apollo-memory-audit-2026-09-18.md#f8]
<a id="apollo-mem-sibling-thread-scratch"></a>- **APOLLO-MEM-SIBLING-THREAD-SCRATCH** — Thread-local scratch in sibling crates is never released [patch] — todo. Code read; bytes to measure first. [../../output/apollo-memory-audit-2026-09-18.md#f9]
<a id="apollo-mem-eager-inverse-2d-3d"></a>- **APOLLO-MEM-EAGER-INVERSE-2D-3D** — 2-D and 3-D plans build inverse and half-axis tables eagerly [patch] — todo. Lazy build as the 1-D base states do; acceptance: forward-only plan bytes measured down. [../../output/apollo-memory-audit-2026-09-18.md#f10]
<a id="apollo-mem-static-layouts"></a>- **APOLLO-MEM-STATIC-LAYOUTS** — Flat direct-mapped statics and Rader orders stored as usize [patch] — todo. Acceptance: static and per-plan bytes measured down with narrower index types. [../../output/apollo-memory-audit-2026-09-18.md#f11]
<a id="apollo-cap-batch-axis-nd"></a>- **APOLLO-CAP-BATCH-AXIS-ND** — Batched, axis-selective and rank-N dense FFT [minor] — in-progress. Slice 1: single-axis transforms at 2-D (array and Leto) and Leto views at 3-D, bitwise the halves of the whole transform. Open: a batched 1-D transform along an axis of a Leto array, and a rank-N plan. Scope and oracle: `../../output/apollo-capability-audit-2026-09-18.md` item 2.
<a id="apollo-cap-normalization-modes"></a>- **APOLLO-CAP-NORMALIZATION-MODES** — Orthonormal and forward normalization; unnormalized inverse at 2-D, 3-D and in api [major] — in-progress. [ADR 0067](docs/adr/0067-normalization-modes.md). Slice 1: the `Direction` strategy (`Forward`, `Inverse`, `InverseUnnormalized`) through the 2-D and 3-D passes and lanes, unnormalized inverses on `FftPlan2D`/`FftPlan3D`, the 1-D NUFFT off normalize-then-multiply. Open: slice 2 (`Normalization::{Backward, Orthonormal, Forward}` through plans and `api`), slice 3 (`kernel::fft_*` to `pub(crate)`).
<a id="apollo-cap-stft-windows"></a>- **APOLLO-CAP-STFT-WINDOWS** — STFT: invert custom windows, a window family, one-sided spectra [minor] — in-progress. Slice 1 (window ownership, [ADR 0069](docs/adr/0069-one-window-for-both-stft-passes.md)): the plan owns one window for both passes (`Window`: Hann, Hamming, Blackman, Tukey; or caller values), `forward_with_window` removed, the inverse refuses windows that do not overlap-add; reconstruction within a derived bound for every family at hops N/2 and N/4. Open: Kaiser (needs a Bessel I0 the crate lacks), one-sided spectra, a `[frames, bins]` view, the GPU plan (Hann only), and the round-trip proptest's 0.5 tolerance. Scope and oracle: `../../output/apollo-capability-audit-2026-09-18.md` item 5.
<a id="apollo-cap-low-precision-truth"></a>- **APOLLO-CAP-LOW-PRECISION-TRUTH** — Fifteen sibling crates run LOW_PRECISION_F32 in f64 while the profile says f32 [patch] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 6.
<a id="apollo-cap-fallible-api"></a>- **APOLLO-CAP-FALLIBLE-API** — A fallible public FFT API in place of panics on empty or mismatched input [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 7.
<a id="apollo-cap-real-split-2-mod-4"></a>- **APOLLO-CAP-REAL-SPLIT-2-MOD-4** — The real half-spectrum split for every even n [patch] — review. Gate widened to even n >= 2; every even n in 2..2048 swept against the widened full transform, n ≡ 2 (mod 4) to 602 plus 1002 and 2050 against the direct sum; the half inverse allocates nothing at even n. Audit item 8: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md).
<a id="apollo-cap-nufft-2d-type3"></a>- **APOLLO-CAP-NUFFT-2D-TYPE3** — NUFFT: a 2-D plan, Type-3, tolerance-driven parameters, fallible constructors [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 9.
<a id="apollo-cap-dctdst-shapes"></a>- **APOLLO-CAP-DCTDST-SHAPES** — DCT/DST: rectangular 2-D and 3-D, orthonormal mode, axis form [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 10.
<a id="apollo-cap-wavelet-coverage"></a>- **APOLLO-CAP-WAVELET-COVERAGE** — Wavelets: more families, non-power-of-two lengths, boundary modes, 2-D, inverse CWT [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 11.
<a id="apollo-cap-gpu-parity"></a>- **APOLLO-CAP-GPU-PARITY** — GPU parity: WGPU real-to-complex; CUDA beyond 1-D f32 powers of two [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 12.
<a id="apollo-cap-python-surface"></a>- **APOLLO-CAP-PYTHON-SURFACE** — Python: NumPy-compatible rfft/irfft, norm= and axes=, more transforms, gil_used [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 13.
<a id="apollo-cap-backend-execution"></a>- **APOLLO-CAP-BACKEND-EXECUTION** — FftBackend cannot execute a transform: CPU fixed to f64, CUDA () plans [minor] [arch] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 14.
<a id="apollo-cap-shift-freq"></a>- **APOLLO-CAP-SHIFT-FREQ** — fftshift/ifftshift beyond 1-D; fftfreq/rfftfreq generic over the scalar [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 15.
<a id="apollo-cap-czt-zoom"></a>- **APOLLO-CAP-CZT-ZOOM** — CZT: a zoom-FFT band constructor and a non-square fast inverse [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 16.
<a id="apollo-cap-frft-fast"></a>- **APOLLO-CAP-FRFT-FAST** — FrFT: an O(N log N) path [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 17.
<a id="apollo-cap-radon-geometries"></a>- **APOLLO-CAP-RADON-GEOMETRIES** — Radon: fan beam, more filters, typed and _into backprojection [minor] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 18.
<a id="apollo-cap-oracle-coverage"></a>- **APOLLO-CAP-ORACLE-COVERAGE** — f32 and complex-input oracle sweeps; the sweep must not tolerate refusals [patch] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 19.
<a id="apollo-cap-book-chapters"></a>- **APOLLO-CAP-BOOK-CHAPTERS** — Book chapters for 13 transform crates, samples compiled rather than ignored [patch] — todo. Scope, non-goals and acceptance oracle: [../../output/apollo-capability-audit-2026-09-18.md](../../output/apollo-capability-audit-2026-09-18.md) item 20.

<a id="atlas-apollo-wider-isa"></a>
## ATLAS-APOLLO-WIDER-ISA-2026-08-28 — The AVX-512 path for the base kernel [arch] — blocked
- **Blocked:** no AVX-512 hardware.
- **The blocker is a fact about the machine, not a dependency:** this host is an Arrow Lake Core Ultra 9 285K and reports `avx512f: false`, probed rather than assumed. The path can be built and proven correct here; it cannot be proven faster. **Re-open trigger:** access to an AVX-512 host, or a decision to merge on SDE correctness plus the arithmetic case.
- **Why it is the live lever** (`gap_audit.md#wider-isa`): eight f64 lanes are four complex per register, and thirty-two registers rather than sixteen — an eightfold change in headroom for a working set measured in rows. That is the quantity that defeated the across-instance layout, which wants sixteen live values on AVX2 and spills (1774 against 518 TSC) but only eight of thirty-two at the wider width. Its advantage, 16 row multiplies against our 64, is built and preserved under `docs/experiments/`.
- **What blocks it in our own code:** the kernels *require* four lanes at seventeen sites across six modules — `exact_lanes_supported::<4>`, `vectorize_lanes::<4>`, and a `LANE_COUNT != 4` early return in the base kernel, the batched boundary, the N=16 codelet, and both resident row kernels — because every address map is written in chunks of two complex samples. An AVX-512 host therefore selects AVX2 everywhere and runs at half width, so the wider ISA buys nothing rather than something.
- **The provider side is ready:** hermes #98 closed the last permute gap, so a width-generic kernel would not fall to a stack-capture default at the wider width.
- **Order if pursued:** parameterize over complex-per-register holding four-lane behaviour byte-identical and confirm on this host that it is free (the row-length precedent says it can be); restore the across-instance layout at eight lanes; measure elsewhere.

<a id="atlas-apollo-branch-debt"></a>
## ATLAS-APOLLO-BRANCH-DEBT-2026-08-27 — Eight stale local branches hold unique patches — blocked
- **Blocked:** the seven remote refs are pure duplication and the session tool refused their deletion; pending the command `git push origin --delete cascade/hermes-07 codex/apollo-arch-006-junk-drawer-rename codex/fix-apollo-package-sources feat/apollo-benchmark-generator fix/apollo-large-composite-wiring perf/apollo-f32-rader-narrowing style/apollo-butterfly-lint-consolidation-217`; re-open trigger: the refs are gone, then this item closes.
- **Classified 2026-09-15:** these are the branches ATLAS-APOLLO-BRANCH-INVENTORY-2026-08-28 swept on 2026-08-29 (eed77fff) — merged as #96, #74, #95 or closed as #86, #103, #92, and the style branch whose approach the sweep rejected ([its item](#atlas-apollo-butterfly-blanket-allows)); the local copies are gone and only the remote refs remain. fix/apollo-sht-thread-local-lint is gone on both sides.
- **Reconciled:** `codex/fix-atlas-sha` contained only a Hermes 0.6 manifest and lock advance; main requires Hermes 0.7 and locks `08306b94`, so both local and remote refs were deleted as superseded on 2026-08-27.
- **Reconciled:** `fix/apollo-fft-workspace-buffers` supplied the four retained Leto GPU staging entry points now in PR #150. Merged Leto `fb70cb6` initializes final Mnemosyne output storage once; real-device parity, stable-address, and failure-atomic shape tests pass. The stale local and remote refs were deleted. Its Winograd blob remains on `cascade/hermes-07` and `fix/apollo-large-composite-wiring`; its lockfile was derived state.
- **Delivered:** PR #150 merged as `0536c9c8`. Its code revision `14993ed9` passed locked checks, strict Clippy, 482/482 Nextest, 2/2 doctests, warning-denied Rustdoc, provider audit, 223/223 interop patch SemVer checks, and 196/196 FFT minor SemVer checks. The 12-case GPU benchmark smoke completed; its single samples are not timing evidence.
- **Claim released:** 2026-09-15, the last update 2026-08-27 older than the stale-claim window; reclaim by re-syncing the board.

<a id="r"></a>
## Rust crate publication aliases [patch] — blocked
- **Blocked:** release authority.
- Owner: Codex `/root`; scope: root dependency identities, clean lockfile, registry package verification, and crates.io publication readiness.
- Acceptance: clean metadata and package archives resolve Mnemosyne and Moirai through their published package identities without changing Rust imports; the workspace gate remains green and reusable crates publish in dependency order through the OIDC release workflow.
- Status: the exact external graph resolves the current Mnemosyne, Moirai, Hermes, Leto, and Hephaestus sources; format, locked `apollo-fft` check, and 400/400 focused Nextest cases pass. The first archive dry run exposed sibling-source includes in `apollo-fft`; the primitive table now crosses the package boundary through `apollo-fft-macros` 0.2.0 code generation and runtime-only integer math is package-owned. Exact package verification, hosted verification, and merge remain.
- **State (2026-09-01, Claude `/root`, claim four weeks stale).** The workspace half is done: `mnemosyne = { package = "mnemosyne-memory" }` and `moirai = { package = "moirai-runtime" }` bind the published identities at the root (`0917a6a6`) without changing imports, and the gate is green. The remaining half — publishing through the OIDC release workflow — is the release state, which needs explicit authority; re-open trigger: a release authorization naming the crates.

# Done

One line an item: the anchor, the identity, the outcome with its commit or PR; the narrative lives in git. Ordered by the hash of the anchor so concurrent records land apart; find an item by its ID.

<a id="atlas-apollo-f32-n32768-stages"></a>- **ATLAS-APOLLO-F32-N32768-STAGES-2026-08-31** — Attribute the remaining large-f32 stage deficit [patch] [perf]. **Delivered:** source `d1dda1bf`, PR #219, merge `8ef1e5d3`; the planar combine median falls 40.06% and the complete instrumented route 8.66%, reaching local f32 N = 32,768 parity with RustFFT while preserving value, route, and zero-warm-allocation semantics.
<a id="atlas-apollo-codelet-selection-unmeasured"></a>- **ATLAS-APOLLO-CODELET-SELECTION-UNMEASURED-2026-09-03** — The generated-codelet arm was chosen without measurement; both scalars now follow the measurement [patch] [perf]. **Finding.** `use_generated_codelet_plan` routed 25 lengths to `ShortWinograd` ahead of any composite decomposition, and nothing checked that the codelet was the faster of the two. `traits.rs` gates every consumer through that one predicate, so the plan, the free dispatcher and the const-generic executor all took it.
<a id="apollo-single-pass-512-four-lanes"></a>- **APOLLO-SINGLE-PASS-512-FOUR-LANES** — Measure the sixteen-row 512 base at four lanes [patch] [perf]. [#421](https://github.com/ryancinsight/apollo/pull/421): the restriction lifted, `f64` 512 0.94 / 0.98 of RustFFT from 1.03 to 1.05, the two-block form of the radix step deleted (`../../output/apollo-base128/small_sizes_base512four_run*_2026-09-11.txt`); ADR 0061 revised.
<a id="apollo-planar-seam-staging"></a>- **APOLLO-PLANAR-SEAM-STAGING** — Stage each seam block through a contiguous buffer [patch] [perf]. **Outcome:** [ADR 0058](docs/adr/0058-planar-sink-staging.md) Accepted: the sink staged through a contiguous block up to `2^16`, placement-independent (16384 `f64` 30k from 30k to 82k cycles, 65536 `f32` 76k to 81k from 124k to 150k); census `f64` 4096 8.8 to 7.8 µs, 16384 37.7 to 33.8, 65536 185.9 to 175.8; the source kept direct on measurement; PR #360 merged 2026-09-09.
<a id="atlas-apollo-main-red-butterfly-globs"></a>- **ATLAS-APOLLO-MAIN-RED-BUTTERFLY-GLOBS-2026-09-01** — Explicit butterfly intrinsic imports [patch]. Delivered by PR #242, PR #257.
<a id="d19-"></a>- **D19-close-eunomia-migration-regressions [minor] [arch] — done** — . Owner: Codex `/root/athena_backend_audit`; scope: Apollo's native benchmark build and execution instrument, representative FFT workload regimes, and synchronized verification records. Comparator thresholds, transform production code, and unrelated provider APIs are non-goals.
<a id="atlas-apollo-storage-route-composite"></a>- **ATLAS-APOLLO-STORAGE-ROUTE-COMPOSITE-2026-09-02** — The half storage route took the plan at every length, including the ones it loses on [patch] [perf]. **The storage route had the defect its own scalar had just been cured of.** `execute_compact_storage` resolved `get_1d_plan` for every length past the register-resident codelets. The plan is the faster route only for powers of two (`ATLAS-APOLLO-PLAN-UNDERSELECTS-COMPOSITE`), so composite-length half transforms paid the same 21-29% penalty that `ATLAS-APOLLO-ONESHOT-MISSES-THE-PLAN` had just removed from `Complex32`. The probe could not see it: its sweep was 8, 16, 32, 64, 128, 256, 512 — every length a power of two, the blind spot named in `gap_audit.md#length-class-split` three hours earlier, still open in the instrument that named it.
<a id="atlas-apollo-isa-fork"></a>- **ATLAS-APOLLO-ISA-FORK-2026-08-25** — Retire the per-ISA fork onto the Hermes seam [arch] — in-progress (eight slices delivered 2026-09-02; integrator Claude; lease: `stockham/avx/`, `stockham/butterfly/`, `stockham/precision/`). **Outcome:** `apollo-fft` stops carrying its own AVX2 and AVX-512 intrinsics and reaches lane-parallel CPU work through `hermes-simd`, which is the Atlas owner of that bounded context.
<a id="apollo-n64-lane-pass"></a>- **APOLLO-N64-LANE-PASS-2026-09-08** — The N=64 lane pass is the stack's most-executed kernel and is unmeasured in its own regime [minor] [perf] (shipped arm confirmed; attribution corrected). **Integrator:** claude-fable-5.1; **branch:** `perf/apollo-n64-lane-pass`, authored through the git API — both apollo trees were held by live peers.
<a id="atlas-apollo-instance-major"></a>- **ATLAS-APOLLO-INSTANCE-MAJOR-2026-08-29** — The across-instance layout ships [perf] [arch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-real-half-api"></a>- **ATLAS-APOLLO-REAL-HALF-API-2026-08-26** — Expose the n/2+1 forward spectrum [minor]. Closed; the delivery record is in git history.
<a id="apollo-2048-f64-column-first"></a>- **APOLLO-2048-F64-COLUMN-FIRST** — 2048 at four lanes on the eight-block column-first form [patch] [perf]. [#446](https://github.com/ryancinsight/apollo/pull/446): the plan selects the eight 256-blocks under the radix-8 pass at 2048 at either width (kernels unchanged; the 512 base builds at 512 and 4096 only); `f64` 2048 0.95 / 0.92 of RustFFT on the performance core from 1.04 / 1.00 and 0.98 / 0.97 on the efficiency core from 1.07 / 1.09 (9% and 9% under the four 512-block sink form), no other length slower (`../../output/apollo-base128/small_sizes_eight2048f64_run*_2026-09-14.txt`); ADR 0061 note.
<a id="apollo-provider-identity"></a>- **APOLLO-PROVIDER-IDENTITY-2026-09-04** — Four providers resolve through two sources, and the merged pins cannot be dropped alone [patch] [arch]. **Closed 2026-09-17:** Moirai dropped its Melinoe revision pin ([Moirai#393](https://github.com/ryancinsight/Moirai/pull/393)), apollo drops its matching quarantine pin and advances its git Moirai to `b94f3ed7`; the lock guard reports 0 sources in excess of one per repository and `.provider-identity-baseline` is 0.
<a id="apollo-column-route-f64-180"></a>- **APOLLO-COLUMN-ROUTE-F64-180** — The 180 column route at two complexes a register [minor] [perf]. [#437](https://github.com/ryancinsight/apollo/pull/437): the six-by-six form of the 36-point kernel (RustFFT's `Butterfly36Avx64` shape) selected by the register's complex count, the column pass and interleave width-generic; `f64` 180 1.06 / 0.68 of RustFFT on the performance core from 1.48 to 1.52 (RustFFT's reading swung 146 to 219 us) and 0.95 / 0.94 on the efficiency core from 1.27, 26 to 32% under the composite route (`../../output/apollo-base128/small_sizes_column180f64_run*_2026-09-11.txt`); ADR 0062 revised. Residue: the kernel's 52 stack moves (thirty-six registers of two through sixteen).
<a id="u"></a>- **Unused CPU marker aliases [major] — done** — . [x] [major] D9-remove-cpu-marker-aliases (owner Codex, completed 2026-07-16; scope fourteen GPU transport manifests, ADR 0033, tests, and PM records): delete definition-only `CpuTransformMarker` aliases. Acceptance: zero workspace references, no compatibility exports, dependency direction remains provider-first, package checks and default Nextest pass, and the pre-1.0 surface deletion is documented. All-targets checks, warning-denied Clippy, provider audit, and 382 default-feature tests pass. Each affected package advances to its next pre-1.0 minor release.
<a id="atlas-apollo-compose-arena-miri"></a>- **ATLAS-APOLLO-COMPOSE-ARENA-MIRI** — Cover the compose arena's raw-pointer pattern with miri [patch]. Closed; the delivery record is in git history.
<a id="apollo-planar-transpose-staged-sweep"></a>- **APOLLO-PLANAR-TRANSPOSE-STAGED-SWEEP** — Feed the second set's first sweep from a staged transpose [patch] [perf] — done 2026-09-10 (rejected). **Outcome:** built (a staged planar source seam in the pass, the first frequency sweep block by block with an in-sweep tile transpose into the staging buffer, a route selector in the driver, a bitwise differential test) and rejected on measurement: the sweep's tiles are strided rows, so the block, one tile width of columns, is the unit, and the per-row-set cost of one-vector blocks outweighs the L2 traffic saved: `f1` 32768 `f32` 131k cycles against the transpose pass plus sweep 48k, 2048 `f32` 7.7k against 2.5k, `rustfft_comparison` 32768 `f32` 63 against 40.5 µs (`../../output/apollo-planar-rectangular/staged*`). Kept: the sweeps beside their stage sets (`dit.rs`, `dif.rs`, `sink.rs`), and the out-of-place transpose's per-tile closure made an in-frame function, which the block fill exposed at seven times the in-frame cost (the odd remainder tile of a sixteen-lane `f32` host pays it in production). ADR 0060 carries the alternative.
<a id="apollo-128-as-four-rows"></a>- **APOLLO-128-AS-FOUR-ROWS** — Measure the 128 base as four 32-sample rows [patch] [perf]. [#424](https://github.com/ryancinsight/apollo/pull/424): the 128 state an enum over the two shapes selected by the native width — four 32-sample rows at eight lanes (3% faster under aligned tables), eight 16-sample rows at four (the four-row form 1 to 2% slower there); ADR 0061 revised.
<a id="apollo-rader-half-cyclic-f32"></a>- **APOLLO-RADER-HALF-CYCLIC-F32** — The half-cyclic Rader composition at n = 101 runs scalar passes and slow 50-point convolutions [patch] [perf]. [#427](https://github.com/ryancinsight/apollo/pull/427): the selector takes the half-cyclic form only where `n - 1` is not 7-smooth, measured against the full form at every probe prime for both scalars (`../../output/apollo-base128/rader_backends_2026-09-11.md`); `f64` 101 0.68 / 0.62 of RustFFT from 1.08, `f32` 0.70 / 0.67 from 1.96. The half-cyclic passes (scalar split and recombine) still serve 83 and 107: [the composite census](#apollo-composite-radix-census) carries that residue.
<a id="atlas-apollo-batched-1d-unreachable"></a>- **ATLAS-APOLLO-BATCHED-1D-UNREACHABLE-2026-08-26** — The batched layout is dead on the 1-D route [arch] — done 2026-08-26 (PR #125). Delivered by PR #168, PR #119, `26fc4ab8`.
<a id="apollo-fft-hephaestus-cutover"></a>- **APOLLO-FFT-HEPHAESTUS-CUTOVER-2026-08-28** — Delete consumer-owned WGPU FFT [major] [arch] [perf] — done 2026-08-29 (PR #176). Delivered by `f981908f`, `7c063e27`.
<a id="atlas-apollo-fft-1024-route"></a>- **ATLAS-APOLLO-FFT-1024-ROUTE-2026-08-31** — Align benchmark codegen with release [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-base-kernel-lane-width"></a>- **ATLAS-APOLLO-BASE-KERNEL-LANE-WIDTH-2026-08-29** — The base kernel runs a four-byte scalar at half native width [perf]. Delivered by `958fbfb2`.
<a id="atlas-apollo-bench-gate-noise"></a>- **ATLAS-APOLLO-BENCH-GATE-NOISE-2026-08-28** — The benchmark gate times on shared runners [patch]. Landed as b255df12 (`ci(bench): Keep performance measurements local`): the shared-runner wall-clock gate (`benchmark-regression.yml`) is deleted; CI keeps the single-iteration bench smoke and regression evidence comes from the pinned local probes attached to each PR, the first option the item preferred.
<a id="apollo-planar-split-vector-deint"></a>- **APOLLO-PLANAR-SPLIT-VECTOR-DEINT** — Vectorize the odd-power decimation into the planes [patch] [perf]. PR [#363](https://github.com/ryancinsight/apollo/pull/363): `DeinterleaveDecimatedRows` through the pair and sub-lane decimations (ADR 0054 revision); `deint` `f64` 2048 4.4k to 2.4k cycles, `f32` 3.5k to 1.0k. The combine then led the seams, taken by [rectangular odd powers](#apollo-planar-rectangular-odd-powers).
<a id="apollo-twiddless-fft-evaluation"></a>- **APOLLO-TWIDDLESS-FFT-EVALUATION** — Measure the twiddless FFT against its butterfly isomorph [patch]. **Outcome:** [ADR 0052](docs/adr/0052-twiddless-fft-evaluation.md) Rejected: 2.4 to 10.7 times slower than the plan at every length but 65536; PR #349 merged 2026-09-08.
<a id="apollo-real-3d-full-routing"></a>- **APOLLO-REAL-3D-FULL-ROUTING** — Route the full-spectrum 3-D real entries through the half volume [minor] [perf]. [#476](https://github.com/ryancinsight/apollo/pull/476) (draft, stacked on #474): the five full-spectrum 3-D real entries routed through the half volume where the split admits `nz` — the half staged in the 2-D role, the volume written once in parallel lane blocks, the inverses packing the lower bins — bit-identical to the pair where routed; the expansion and packs shared with the 2-D pair in `real_storage/expand.rs`. Floors from `output/probe3d`: caller-owned routes from 8-long lanes (4-long ran 0.83x / 0.91-0.96x), owned forward from 4 MiB, owned inverse none. Census (`engine_census_3d_routed_2026-09-15.txt`), widened over routed by the minimum: 4096x4x4 1.03x / 2.48x; 64x64x64 1.58x / 3.30x.
<a id="atlas-apollo-qft-fft"></a>- **ATLAS-APOLLO-QFT-FFT-2026-08-31** — Route reusable QFT plans through Apollo FFT [perf] [patch]. Delivered by PR #213, `39420cca`, `f3138f50`, `3e2c2edc`.
<a id="atlas-apollo-planar-radix8"></a>- **ATLAS-APOLLO-PLANAR-RADIX8-2026-08-27** — Deeper stage fusion in the planar batched kernel [arch] (radix-8 declined by both instruments, the twiddle fold delivered instead). Closed; the delivery record is in git history.
<a id="atlas-apollo-plan-underselects-composite"></a>- **ATLAS-APOLLO-PLAN-UNDERSELECTS-COMPOSITE-2026-09-02** — The cached plan is slower than the ad-hoc dispatcher for composite lengths [patch] [perf]. **Finding.** `FftPlan1D::forward_complex_slice_inplace` and `mixed_radix::forward_inplace` compute the same transform by different routes, and the plan — the artifact that exists precisely to hold the precomputed decomposition — is the *slower* of the two for non-power-of-two lengths. Both compiled into one binary, arms alternating in one process, `f32` forward, performance core, minimum of 100 pinned samples: | n | free function | plan | | | --- | --- | --- | --- | | 96 | 72.73 ns | 72.50 | wash | | 100 | 101.40 | 130.65 | plan +29% | | 128 | 112.40 | 60.93 | plan -46% | | 256 | 226.16 | 146.71 | plan -35% | | 384 | 281.79 | 354.34 | plan +26% | | 512 | 395.69 | 323.54 | plan -18% | | 1000 | 1036.65 | 1025.36 | wash |
<a id="apollo-melinoe-executor-receiver"></a>- **APOLLO-MELINOE-EXECUTOR-RECEIVER-2026-09-11** — Track unpublished provider receiver construction [patch]. **Closed 2026-09-17:** Melinoe `9b7039f` (merged in `059e5891`, its main) makes `ParallelExecutor::run_indexed` an associated function, so no receiver is fabricated and `shared_instance` is gone; its non-zero-sized registration test and the four other executor tests pass under Miri (`cargo +nightly miri test --test partition -- executor`, 5/5 at `059e5891`); apollo already resolves Melinoe at `059e5891`.
<a id="atlas-apollo-twiddle-accuracy"></a>- **ATLAS-APOLLO-TWIDDLE-ACCURACY-2026-08-25 [patch]**. Closed; the delivery record is in git history.
<a id="✅ atlas-apollo-se"></a>- **ATLAS-APOLLO-SEMVER-BASELINE-UNBUILDABLE-2026-08-29** — cargo-semver-checks cannot build the released baseline [patch]. Delivered by `378081ec`.
<a id="apollo-lane-unit-tasks"></a>- **APOLLO-LANE-UNIT-TASKS-2026-09-15** — Lane passes decide scheduling through moirai unit tasks [patch] [perf]. #460: `lanes::each` (under `LaneTasks`) and `lanes::paired` (under `WorkBytes<PARALLEL_BYTES>`) run on moirai `for_each_unit_task_mut_with`; apollo's `TASK_BYTES`, `lanes_per_task` and hand branch deleted; workspace clippy clean, 1,525 nextest passed; `lane_threshold` per-run minima within 1%.
<a id="atlas-apollo-f16-fused-small-bases"></a>- **ATLAS-APOLLO-F16-FUSED-SMALL-BASES-2026-09-02** — Fuse half conversion into the register-resident codelets [patch] [perf] (falsified). **Built and falsified 2026-09-02 (`gap_audit.md#half-fusion-not-established`).** The upstream blocker was removed (hermes gained a kernel-declared F16C frame, eunomia's bulk dispatch became inlinable) and apollo gained the fused codelet. It is correct and it is not faster: measured with both paths in one binary and the arms alternating in one process, n = 16 fused 9.50/9.72 ns against staged 9.09/8.65, n = 32 a wash. The ceiling assumed three trips to memory; at 128 to 256 bytes they are three trips to L1, and the fused body costs more in register pressure than they cost in traffic. Apollo's codelet and hermes' const are reverted; eunomia's `#[inline]` stands as neutral and independently sound.
<a id="atlas-apollo-fused-split-loads"></a>- **ATLAS-APOLLO-FUSED-SPLIT-LOADS-2026-09-01** — The gather rides the phase-one loads out [patch] [perf] (falsified). Closed; the delivery record is in git history.
<a id="r-2"></a>- **Release 0.16.0 eligibility [arch] — done** — . [x] Pin the toolchain, current compatible third-party versions, and exact Atlas provider revisions in one reproducible lock graph.
<a id="apollo-3d-return-transposes"></a>- **APOLLO-3D-RETURN-TRANSPOSES-2026-09-10** — A 3-D forward moves the volume four times for two axis passes [minor] [perf]. **Integrator:** claude-fable-5.1; **branch:** `perf/apollo-3d-return-transposes`.
<a id="atlas-apollo-real-split"></a>- **ATLAS-APOLLO-REAL-SPLIT-2026-08-25** — Real input costs a half-length transform [patch]. Closed; the delivery record is in git history.
<a id="apollo-planar-stage-sweeps"></a>- **APOLLO-PLANAR-STAGE-SWEEPS** — Run several stages per trip through the planes [patch] [perf]. **Outcome:** [ADR 0056](docs/adr/0056-planar-stage-sweeps.md) Accepted: four stages per sweep over 16-row tiles blocked to 16 KiB, both stage sets; the plain sweep halves the pass cost (14.6 to 7.8 cycles per quad); whole transform 7 to 14% faster at 16384 and 65536 in both precisions; per-sweep attribution `t1..t3`, `f1..f3`; evidence `../../output/apollo-planar-sweeps/`; PR #354 merged 2026-09-09.
<a id="apollo-table-alignment"></a>- **APOLLO-TABLE-ALIGNMENT** — Allocate the vector-read twiddle tables on a cache line [patch] [perf] (no effect). **Outcome:** eight process runs of the section probe printing the fold tables' line offsets (`../../output/apollo-planar-rectangular/tables_*.txt`): the fold sweep at 16384 reads 24.9k to 25.4k cycles `f64` and 13.2k to 13.5k `f32` with the tables at 0, 16 or 48 bytes into a line, and the 65536 spread (126k to 138k) has no correlation with the offsets; one register load per row is too few to pay the split. The tables stay as allocated.
<a id="apollo-four-step-cache-tile"></a>- **APOLLO-FOUR-STEP-CACHE-TILE** — Evaluate cache-line fused traversal [patch]. [PR 336](https://github.com/ryancinsight/apollo/pull/336), `7976ab49`; [Rejected ADR 0050](docs/adr/0050-cache-line-fused-traversal.md) records no supported performance gain, executable/cold-allocation growth and restored production.
<a id="atlas-apollo-peak-memory"></a>- **ATLAS-APOLLO-PEAK-MEMORY-2026-08-25** — Measure peak working set against PhastFT [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-safety-comment-ratchet"></a>- **ATLAS-APOLLO-SAFETY-COMMENT-RATCHET** — Ratchet SAFETY-comment coverage over unsafe sites [patch]. Reached zero in [#472](https://github.com/ryancinsight/apollo/pull/472) after the Cook-Toom slice (#466): `scripts/safety_ratchet.py check` counts 0 uncommented `unsafe` blocks across the workspace crates against a baseline of 0, from 406 in 35 files when the instrument landed (2026-09-01). Three release-mode soundness holes surfaced on the way and are fixed in the same PR — the Stockham entries, the two-by-prime entries and the Bluestein fold checked their lengths only in debug before slicing unchecked. The check stays wired in the `ci` workflow as the guard against regrowth.
<a id="apollo-ntt-circle-group-lengths"></a>- **APOLLO-NTT-CIRCLE-GROUP-LENGTHS** — Admit `n | p + 1` transform lengths [minor]. [#480](https://github.com/ryancinsight/apollo/pull/480): `CircleNttPlan` — the circle FFT of Haböck, Levit, Papini over the standard position coset of the circle group, exact in both directions to `2^16`, its basis pinned against unit coefficient vectors and the pointwise product against a symbolic schoolbook product modulo the domain's vanishing polynomial ([ADR 0065](docs/adr/0065-circle-group-ntt-lengths.md)); `benches/circle.rs` reads `N log N` (4.5-4.8x per 4x length, 33.7 µs at 1024 to 16.2 ms at 262144, `ntt_circle_2026-09-15.txt`), scalar butterflies 1.2-1.7x behind the cyclic route's lane kernel.
<a id="atlas-apollo-base-64-switch-retired"></a>- **ATLAS-APOLLO-BASE-64-SWITCH-RETIRED-2026-08-30** — A measured constant outlived its premises [patch] [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-ecoqos-premise"></a>- **ATLAS-APOLLO-ECOQOS-PREMISE-2026-09-01** — Re-establish or withdraw the EcoQoS root cause [patch] — done 2026-09-01 (withdrawn, merged 0ed7660a). Delivered by PR #245.
<a id="o-2"></a>- **Open performance target**. Superseded: the parent [ATLAS-APOLLO-BEAT-THE-REFERENCES-2026-09-03](#atlas-apollo-beat-the-references) owns the RustFFT matrix, and 512 and 4096 run the base-512 route (`base512` in `plan/fft/dimension_1d/dynamic_impl.rs`), not a Stockham base.
<a id="atlas-apollo-butterfly-blanket-allows"></a>- **ATLAS-APOLLO-BUTTERFLY-BLANKET-ALLOWS-2026-08-29** — Module-wide lint allows in the butterfly kernels [patch]. Closed; the delivery record is in git history.
<a id="apollo-composite-radix-census"></a>- **APOLLO-COMPOSITE-RADIX-CENSUS** — Census the composite radix route against RustFFT at 180 and 384 [patch] [perf]. Census recorded (`../../output/apollo-base128/composite_census_2026-09-11.md`): the per-register butterflies cost what RustFFT pays (R3 30 against 34 instructions, R4 39 against 44, R5 56 to 64 against 63, no spills in the inner loops); the gap is the pass count — four Stockham passes at 180 and five at 384 against RustFFT's two (a 36- or 128-point in-register base butterfly over columns under one column pass). Filed as [384 as three 128-blocks](#apollo-384-as-three-128-blocks) and [the codelets over lanes](#apollo-codelets-over-lanes).
<a id="atlas-apollo-composite-radix-still-silent"></a>- **ATLAS-APOLLO-COMPOSITE-RADIX-STILL-SILENT-2026-08-29** — 92 of the 109 broken FFT lengths still return wrong answers silently [major]. Closed; the delivery record is in git history.
<a id="atlas-apollo-base-ops"></a>- **ATLAS-APOLLO-BASE-OPS-2026-08-29** — Instruction model for the base kernel [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-pot-planar"></a>- **ATLAS-APOLLO-POT-PLANAR-2026-08-25** — Planar layout for the power-of-two kernel family [arch] (premise falsified). **Outcome:** the power-of-two f64 path closes most of the measured 2.0x-3.5x gap against PhastFT, by removing the per-butterfly cross-lane shuffles that the interleaved `Complex64` layout forces on every complex multiply.
<a id="apollo-private-rustdoc-links"></a>- **APOLLO-PRIVATE-RUSTDOC-LINKS** — Repair private FFT documentation references [patch]. `cf1ddd68` corrects 20 private documentation warnings in 12 FFT files with no non-documentation changes; warning-denied private/hidden Rustdoc passes on nightly-2026-08-01 with lock `F9720DFD…2E90197`; [gate evidence](../../output/apollo-square-transpose/integration/final-gates/documentation-closure.json).
<a id="apollo-lane-unit-tasks-review"></a>- **APOLLO-LANE-UNIT-TASKS-REVIEW** — Post-merge review of the lane unit-task migration [patch]. Reviewed [#460](https://github.com/ryancinsight/apollo/pull/460) at 7d82a896 against moirai f89c9acf: `paired` derives each group's input offset from `first_unit = task · per_task` exactly as `for_each_unit_task_mut_with` indexes its runs, `WorkBytes<PARALLEL_BYTES>` reproduces the removed `lanes · pair_bytes >= PARALLEL_BYTES` branch, and `each` keeps `LaneTasks` through the default `parallelize_work`; the lock it brings resolves melinoe through two source strings (excess 1), bounded by the provider baseline #459 set. Its record [#463](https://github.com/ryancinsight/apollo/pull/463) leaves the done heading in the open section; the next compaction folds it. Findings: none.
<a id="atlas-apollo-rader-59-variance"></a>- **ATLAS-APOLLO-RADER-59-VARIANCE-2026-08-29** — Rader at n = 59 is unstable across runs [patch]. Landed 0ba9c504: non-smooth `m < 128` takes the half-cyclic backend (`prefers_bluestein_for_rader`, `rader/mod.rs`) with n = 167 retained as a control; reviewed 2026-09-15 at origin/main — the rule, its doc-carried medians and the 59/83/107 tests agree with this record. Findings: none.
<a id="apollo-real-3d-pass-count"></a>- **APOLLO-REAL-3D-PASS-COUNT** — Cut the passes the 64 cubed real pair makes over its volume [patch] [perf]. **Delivered:** #490 (the z inverse in one sweep), #491 (the parked-pool wake measured, filed as moirai `MOI-PARKED-POOL-FIRST-REGION-2026-09-17`), and the x-last forward and inverse (two full-volume moves each way): forward 179.5-188.1 to 157.0-160.1 us, inverse 208-213 to 174.6-177.3 us at 64 cubed f64 (`output/apollo-base128/half_pair_phases_2026-09-17.md`); residual scatter cost in [APOLLO-REAL-3D-X-LAST-SCATTER](#apollo-real-3d-x-last-scatter).
<a id="atlas-apollo-sink-permutation"></a>- **ATLAS-APOLLO-SINK-PERMUTATION-2026-08-28** — Carry the sink's reversal on the write side [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-small-size-split"></a>- **ATLAS-APOLLO-SMALL-SIZE-SPLIT-2026-08-28** — Route 256 and 512 through the 128 base [patch]. Closed; the delivery record is in git history.
<a id="apollo-planar-rectangular-odd-powers"></a>- **APOLLO-PLANAR-RECTANGULAR-ODD-POWERS** — Run odd powers through a rectangular planar four-step [patch] [perf]. PR [#365](https://github.com/ryancinsight/apollo/pull/365), [ADR 0060](docs/adr/0060-planar-rectangular-odd-powers.md): `N1 × 2 N1` through the square driver, the split, decimation and combine deleted; section totals `f64` 2048 16.1k to 12.1k cycles. The L1 floor that remained closed as [the L1 route spike](#apollo-planar-l1-route-spike).
<a id="apollo-target-feature-boundary"></a>- **APOLLO-TARGET-FEATURE-BOUNDARY-2026-09-06** — Measure vector frame crossings [patch] [perf]. **Premise.** `n8`/`n16`/`n32`'s `vector_arm` carries `#[target_feature(enable = "avx,fma")]` and its callers do not, so it can never inline and every codelet call is a real call through memory — `dimension_2d`'s axis passes re-cross it once per lane. Hoisting the frame around the lane loop would remove all but one crossing per pass.
<a id="apollo-transpose-cache-geometry"></a>- **APOLLO-TRANSPOSE-CACHE-GEOMETRY** — Model transpose cache-set pressure [patch] (superseded). **Outcome:** the conflict hypothesis was confirmed and closed by measurement rather than counters: the planar route pads every plane row by a cache line (`ROW_PAD`, ADR 0056 and 0057), because an unpadded power-of-two stride put every row of a plane in one L1 set, and the padded stride measured the aliasing away; the remaining transpose cost is the register network's shuffle floor ([the folded-loads item](#apollo-planar-transpose-folded-loads), ADR 0060). No further model is pending.
<a id="atlas-apollo-twiddle-ssot"></a>- **ATLAS-APOLLO-TWIDDLE-SSOT-2026-08-25** — One twiddle builder [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-half-storage-small-stack"></a>- **ATLAS-APOLLO-HALF-STORAGE-SMALL-STACK-2026-09-02** — Register-resident half lengths need no pool [patch] [perf]. **Delivered** (`gap_audit.md#half-storage-small-and-routing`): lengths 2, 4, 8, 16 and 32 widen into a fixed stack array, run the sized codelet the f32 route selects, and narrow back — no thread-local pool borrow, no growth check, no depth bookkeeping, for buffers of at most 256 bytes that the codelet keeps in registers anyway.
<a id="atlas-apollo-oneshot-misses-the-plan"></a>- **ATLAS-APOLLO-ONESHOT-MISSES-THE-PLAN-2026-09-02** — The f32/f64 one-shot entry re-derived its plan on every call [patch] [perf]. **Found while verifying the f16 storage route** (`ATLAS-APOLLO-STORAGE-ROUTE-MISSES-THE-PLAN`). Adding a symmetric control to the promotion probe — `Complex32` through the same `FftPrecision::fft_forward` the storage type uses — showed the f32 arm running *slower* than the half-storage arm at every n >= 64, and tracking the raw-kernel arm almost exactly (110.1 against 113.5 ns at n = 128). The macro-generated fallback called `mixed_radix::forward_inplace`, which re-derives twiddles and the radix decomposition per call; the cached plan holds both. The peer's fix routed `Complex<f16>` through the plan and left its own scalar behind.
<a id="apollo-hephaestus-default"></a>- **APOLLO-HEPHAESTUS-DEFAULT-2026-09-04** — Retire the merged Hephaestus co-evolution pin [patch]. **Result:** Hephaestus resolves once at merged default `f6c9bfdb`; the `dec3d72` query pin is absent while Mnemosyne and Moirai pins remain unchanged.
<a id="apollo-fold-compact-memory"></a>- **APOLLO-FOLD-COMPACT-MEMORY** — Decide the compact fold table below 2^18 by its memory against its cost [patch]. PR [#390](https://github.com/ryancinsight/apollo/pull/390): `COMPACT_FOLD_MIN_LEN` 2^18 to 2^16 (ADR 0059 revision 2026-09-10); at 65536 the compact table reads 0.96/1.00 of the transform and the fold tables go from 1 MiB to 272 KiB per direction, peak working set 1860 to 1086 KB (`../../output/apollo-fold-compact-2026-09-10/`); 16384 stays full.
<a id="d12-"></a>- **D12-remove-winograd-reexport [patch] — done** — . Owner: Codex; scope: `apollo-fft`/macro internal trait paths, `ShortWinogradScalar` canonical module, and synchronized PM records.
<a id="atlas-apollo-hosted-bench-regression"></a>- **ATLAS-APOLLO-HOSTED-BENCH-REGRESSION-2026-08-26** — Resolve post-merge small-kernel regressions [patch] [arch]. Delivered by PR #130, `e12d1ce2`, `bdc68a89`.
<a id="atlas-apollo-book-test"></a>- **ATLAS-APOLLO-BOOK-TEST-2026-08-20 [patch] — in progress — done** — . The executable-book PR #108 exposed a real mdBook 0.5.4 contract defect in the included Apollo examples: cargo build --locked -p apollo-fft passed, but mdBook test could not resolve apollo_fft, eunomia, or leto because the source files lacked explicit extern crate declarations.
<a id="atlas-apollo-measurement-smt-siblings"></a>- **ATLAS-APOLLO-MEASUREMENT-SMT-SIBLINGS-2026-09-02** — The pinned instruments do not know a processor's SMT sibling [patch]. [#465](https://github.com/ryancinsight/apollo/pull/465): `measurement_cores` selects through themis `CpuTopology::smt()` — with a sibling table an arm is the first class member that is not processor 0, shares no core with it and none with an earlier arm; without one the index rule stands — and the census prints each processor’s core; the decision is a pure `choose` tested on a synthetic four-core table (this host has no SMT) and the live topology in both profiles.
<a id="apollo-four-step-profile"></a>- **APOLLO-FOUR-STEP-PROFILE** — Attribute generic FourStep layout cost [patch]. [PR 335](https://github.com/ryancinsight/apollo/pull/335), `dd58067c`; test-only phase attribution identifies layout cost. [ADR 0049](docs/adr/0049-fused-twiddle-transpose.md) records the rejected register candidate, restored production artifact and evidence limits.
<a id="atlas-apollo-avx-stockham-audit"></a>- **ATLAS-APOLLO-AVX-STOCKHAM-AUDIT-2026-08-25** — Decide the AVX Stockham backend's future [arch] — status: in-progress (reopened 2026-09-01). **Reopened 2026-09-01.** The 2026-08-27 verdict below was withdrawn: it rested on a `backend_matrix` table whose two core-class columns were swapped, because the probe pinned to cpu 2 / cpu 12 and labelled by `landed < 8` while this host's performance set is `{0, 1, 10, 11, 12, 13, 22, 23}` (ADR 0043). The instrument now queries the class; the corrected re-run (three runs, ADR 0042) shows **the auto-vectorized scalar backend beating the AVX backend at every probed size and both precisions on performance cores** (f64 2.2–4.4x, f32 1.05–2.1x), while AVX wins on efficiency cores at most sizes. The retirement premise this item opened with is supported, not falsified.
<a id="atlas-apollo-direct-n2-cvxiii"></a>- **ATLAS-APOLLO-DIRECT-N2-CVXIII** — Re-audit the public N=2 performance claim [patch]. Closed; the delivery record is in git history.
<a id="apollo-planar-interleaved-radix-eight"></a>- **APOLLO-PLANAR-INTERLEAVED-RADIX-EIGHT** — Radix-8 butterflies on interleaved rows [minor] [perf] (rejected). **Outcome:** closed on analysis in [ADR 0056](docs/adr/0056-planar-stage-sweeps.md): the sweep halves the bytes per element-stage without touching the arithmetic, the interleaved radix-8 would move a third fewer bytes at 1.7 times the vector operations, and the remaining cost is the seams, which the layout would not remove.
<a id="atlas-apollo-sht-fft"></a>- **ATLAS-APOLLO-SHT-FFT-2026-08-29** — SHT longitude FFT factorization [minor]. Delivered by `a3cfe5cf`.
<a id="atlas-apollo-f32-n96-column-batch"></a>- **ATLAS-APOLLO-F32-N96-COLUMN-BATCH-2026-09-01** — Batch the residual Good-Thomas columns [patch] [perf]. **Outcome:** rejected before production routing; the value-gated exact-eight-lane candidate regressed the release column/scatter phase from 46.07 ns to 56.91 ns (23.53%) because register materialization preserved all 96 scalar CRT stores.
<a id="apollo-four-step-twiddle-retention"></a>- **APOLLO-FOUR-STEP-TWIDDLE-RETENTION** — Acquire only route-consumed stage tables [patch]. Merged [PR 332](https://github.com/ryancinsight/apollo/pull/332), `f69f9c08`; measured retained bytes fall 25–36% at generic even powers with zero warm 1-D allocations. [ADR 0039](docs/adr/0039-one-dimensional-power-of-two-routing.md) records evidence and limits.
<a id="apollo-spectral-peak-estimation-spike"></a>- **APOLLO-SPECTRAL-PEAK-ESTIMATION-SPIKE** — Decide a sub-bin sinusoid parameter estimator [patch]. **Decided:** the three-bin complex-ratio interpolator with Candan's `tan(pi/N)/(pi/N)` correction on the rectangular window, amplitude and phase from the image-corrected kernel solve, and estimate-and-subtract for multi-tone scenes: 5e-10 Hz / 1e-9 V / 3e-9 rad on the 1 V tones (their negative-frequency image floor) and 6e-7 Hz on the 1e-6 V tone (1.7x its Cramer-Rao bound), against 1e-3 to 3e-2 Hz for the Hann candidates and a seed that never resolves for Aboutanios-Mulgrew. Prism FFT excluded: its windows are defined only in Henry, IEEE TIM 69 (2020), not retrievable. See `docs/adr/0066-sub-bin-peak-estimation.md`; the candidates are in git history, the surface is [APOLLO-PEAK-ESTIMATION-SURFACE](#apollo-peak-estimation-surface).
<a id="apollo-generic-pot-executor-silent-skip"></a>- **APOLLO-GENERIC-POT-EXECUTOR-SILENT-SKIP** — The generic power-of-two executor returns without transforming when the plan has no twiddle table [patch] [fix]. [#451](https://github.com/ryancinsight/apollo/pull/451): the sized and generic forward executors take the table through `FftPlan1D::forward_twiddles`, an `expect` on the invariant that a power-of-two plan without a base route owns it; `every_power_of_two_transforms_an_impulse_into_a_flat_spectrum` (both scalars, 2^0 to 2^20) and the normalized-inverse rung test extended to 2^20; on the reproduced defect tree (the 256 base at 2^19 without its arm) both tests fail — the impulse test at the executor invariant (`invariant: a power-of-two plan without a base route owns its forward table`) and the rung round trip likewise.
<a id="atlas-apollo-storage-route-misses-the-plan"></a>- **ATLAS-APOLLO-STORAGE-ROUTE-MISSES-THE-PLAN-2026-09-02** — The storage route reaches a slower kernel family than the plan [patch] [perf]. **Delivered:** `b61a84c5` routes compact storage through the cached f32 plan, retains stack codelets for 2–32, passes 515 native tests plus doctests, and reduces the warm 64–512 route to 1.08–1.34x of the plan baseline.
<a id="apollo-planar-radix-depth"></a>- **APOLLO-PLANAR-RADIX-DEPTH** — Cut the planar stage sets' instructions per element [patch] [perf]. **Outcome:** [ADR 0055](docs/adr/0055-planar-radix-eight-passes.md) Accepted: radix-8 rejected on AVX2, the shared seam-specialized driver kept (8 to 14% at 4096 and 16384 `f64` warm), unit rotations rejected; PR #353 merged 2026-09-08.
<a id="apollo-half-volume-fallback-scratch"></a>- **APOLLO-HALF-VOLUME-FALLBACK-SCRATCH-2026-09-15** — The half-volume real fallback allocates one `nz` lane per scheduled task [patch] [perf]. [PR #456](https://github.com/ryancinsight/apollo/pull/456): `lanes::paired` moves from an `init`/`lane` pair to one `task` closure per scheduled group; the widened z-lane fallback borrows the 2-D thread-local scratch role (`with_view_staging::<_, 3, _>`, disjoint from the x/y passes' 3-D roles by call order) instead of allocating. `the_refused_length_fallback_allocates_nothing_once_warm` fails on origin/main (2 allocations at `[8, 8, 6]`) and passes fixed, `f64`/`f32`/`F16` at `8x8x6` and `64x64x30`.
<a id="apollo-four-step-square-movement"></a>- **APOLLO-FOUR-STEP-SQUARE-MOVEMENT** — Consolidate provider-owned FourStep movement [patch]. [PR 338](https://github.com/ryancinsight/apollo/pull/338), `2ac33b95`; Leto-owned copy movement passes current-provider correctness, API, allocation and size gates, plus independently accepted timing: one supported E-core real-half/262144 gain, no supported regression. [ADR 0040](docs/adr/0040-leto-fft-layout-ownership.md) records cold-memory increases and the remaining competitor losses.
<a id="apollo-ci-pr-change-scope"></a>- **APOLLO-CI-PR-CHANGE-SCOPE** — Run the Rust pipeline only for pull requests that change code [patch]. **Delivered:** [#489](https://github.com/ryancinsight/apollo/pull/489): a `change scope` job diffs a pull request against its base, and when every path is the board, the ADRs or `scripts/` the required `rust workspace` check runs only checkout, the script tests and the SAFETY ratchet. Observed: #488 (scripts only) passed `rust workspace` in 6 s with every Rust step skipped, where #489 (the workflow itself) ran the full path in 519 s. Pushes to main, other paths, an empty diff and a failed scope job run everything.
<a id="✅ atlas-apollo-sh"></a>- **ATLAS-APOLLO-SHAPE1D-PRIVACY** — Privatize Shape1D.n behind its validating constructor [major]. Closed; the delivery record is in git history.
<a id="apollo-f32-16-32-kernel-gap"></a>- **APOLLO-F32-16-32-KERNEL-GAP** — Close the `f32` 16 and 32 kernel gap now the call chain is gone [patch] [perf]. [#433](https://github.com/ryancinsight/apollo/pull/433), [#434](https://github.com/ryancinsight/apollo/pull/434) with hermes [#172](https://github.com/ryancinsight/hermes/pull/172) and [#173](https://github.com/ryancinsight/hermes/pull/173): the rotation as a sign flip (every register kernel: 64 through 512 8 to 13% under their frame-run readings at both scalars and cores), the twiddle rows as direct and swapped lanes loaded opaque through `mul_with_swapped`. `f32` 16 on the efficiency core 0.97 / 0.96 from 1.27, `f32` 32 on the performance core 0.95 / 0.96 from 1.15 to 1.23 (`../../output/apollo-base128/small_sizes_kernelgap*_2026-09-11.txt`); ADR 0061 revised. Residue: `f64` 8 on the performance core 1.11 to 1.22 (the scalar codelet), `f64` 32 on the efficiency core 1.04 to 1.07.
<a id="apollo-local-performance-gate"></a>- **APOLLO-LOCAL-PERFORMANCE-GATE** — Keep timing on controlled local hosts [patch] [arch]. `b255df12`, [PR 338](https://github.com/ryancinsight/apollo/pull/338), merged `2ac33b95`: hosted timing removed; seven bounded smokes and preparation regressions remain in passing required CI. [ADR 0036](docs/adr/0036-native-benchmark-regression-oracle.md) preserves the statistical contract; actionlint remains unavailable locally.
<a id="atlas-apollo-fused-planar-rows"></a>- **ATLAS-APOLLO-FUSED-PLANAR-ROWS** — Planar-register rows with fused transposes [arch] — closed: premise falsified 2026-08-27. **Two independent verdicts, same conclusion.** A design review (merged via PR #147) rejected ADR 0041's construction analytically: the fused two-pass claim lacked an in-place dependency proof (a row transform cannot scatter transposed output over rows not yet read), and the planar fit consumes the complete AVX2 register file. Concurrently the row body was built and measured, falsifying the premise empirically: the implementation kept the five-pass driver (no in-place hazard) and carried the required lane permutations (swap_pairs/swap_adjacent stages — all five oracles green, including the planar-vs-interleaved differential), and still measured **6.3k TSC per row pass against the interleaved kernel's 5.6k** — the boundary deinterleave/interleave networks spend as many shuffle-class ops as the interleaved multiply chain they replace, and the E-core was never shuffle-port-bound. Both register-row shapes cost ~3x a batched streaming pass in arithmetic alone.
<a id="atlas-apollo-combine-sink-512"></a>- **ATLAS-APOLLO-COMBINE-SINK-512-2026-08-31** — Two-level sink for the four-block split [perf]. Delivered by `72bdf90a`, `160d3154`.
<a id="atlas-apollo-crossover-rederive"></a>- **ATLAS-APOLLO-CROSSOVER-REDERIVE-2026-08-26** — Re-derive the 1-D four-step crossover [arch] (one part open). Closed; the delivery record is in git history.
<a id="apollo-gap-audit-compaction"></a>- **APOLLO-GAP-AUDIT-COMPACTION** — Compact gap_audit.md to its open risks [patch]. [#467](https://github.com/ryancinsight/apollo/pull/467): `scripts/compact_gap_audit.py` keeps every anchored finding (heading, lead to a sentence end, re-open and anchor lines, ten lines at most) and the slop-pattern library, drops the 180 unanchored ledger and rerun sections to git; 10577 to 602 lines, 81 anchors preserved, 26 cited anchors resolve, a second run byte-identical.
<a id="atlas-apollo-split-single-pass"></a>- **ATLAS-APOLLO-SPLIT-SINGLE-PASS-2026-08-28** — One decimation pass feeding both plane sets [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-interleaved-batched"></a>- **ATLAS-APOLLO-INTERLEAVED-BATCHED-2026-08-27** — Interleaved batched kernel [arch] (declined by measurement). Closed; the delivery record is in git history.
<a id="apollo-base128-gather-in-loads"></a>- **APOLLO-BASE128-GATHER-IN-LOADS** — Load the split's source directly in the base transform [patch] [perf]. **Outcome:** delivered as slice 7 of [the two-pass base](#apollo-l1-base-two-pass): at four lanes every block reads its stride-`blocks` subsequence out of the parent and the gather pass is gone (`f64` 1024 from 1.33 to 1.15 to 1.23 of RustFFT); at eight lanes the in-register extraction measured 2 to 4% slower than the gather, so that width keeps it. The bound recorded here (the combining block must gather at 1024) fell to a scratch layout that keeps the even half's halves beside the peers.
<a id="atlas-apollo-batched-seams"></a>- **ATLAS-APOLLO-BATCHED-SEAMS-2026-08-27** — Fold the batched driver's permutation seams [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-four-step-layout-sensitivity"></a>- **ATLAS-APOLLO-FOUR-STEP-LAYOUT-SENSITIVITY-2026-08-26** — Four-step 8x slower in one process than another [arch] (root-caused: not layout). **Root cause: the hybrid scheduler.** The host is a Core Ultra 9 285K (8 P-cores, 16 E-cores). Windows hands benchmark child processes EcoQoS — efficiency cores at efficiency frequency — and instrumenting the census process caught the batched kernel executing exclusively on E-cores (CPUs 8 through 21, thread wandering among them), every call slow, while the same binary elsewhere ran unthrottled. Every "process-dependent" four-step anomaly in this audit, the 2x same-code timing swings included, was scheduling.
<a id="✅ atlas-apollo-sm"></a>- **ATLAS-APOLLO-SMALL-F32-BASES-2026-09-01** — f32 n = 16 and 32 have no live vector kernel [patch] [perf]. Closed; the delivery record is in git history.
<a id="apollo-peak-estimation-surface"></a>- **APOLLO-PEAK-ESTIMATION-SURFACE** — A sub-bin peak parameter estimator on apollo-stft [minor]. **Delivered:** `apollo_stft::estimate_peaks` over `eunomia::RealField` (ADR 0066 revised to the inverse-tangent closure), mirror bins refused, a cancellation-free kernel with the image argument wrapped modulo `N`; every read of the ADR scene checked against its derived bound on FFT and direct-DFT spectra (8.01e-10 / 7.59e-10 Hz outer, 5.96e-7 Hz middle), per-scalar boundary and tone cases; spike candidates deleted. [PR #494](https://github.com/ryancinsight/apollo/pull/494).
<a id="atlas-apollo-interleaved-codelets"></a>- **ATLAS-APOLLO-INTERLEAVED-CODELETS-2026-08-27** — Interleaved register-resident small-N codelets [arch] (measurement redirected the wiring). Closed; the delivery record is in git history.
<a id="apollo-planar-transpose-sixteen-lanes"></a>- **APOLLO-PLANAR-TRANSPOSE-SIXTEEN-LANES** — Let the planar transposes take the eight-wide pad at sixteen lanes [patch] [perf]. **Outcome:** PR #371. Both transpose kernels address tile rows by element offset and guard rows and columns only, so the eight-wide pad is taken at sixteen lanes; the relabeled-transpose model at sixteen lanes and the scalar reference cover it, and the shape `(32, 64, 4)` pins a stride no eight-lane width divides.
<a id="atlas-apollo-transform-algos"></a>- **ATLAS-APOLLO-TRANSFORM-ALGOS-2026-08-28** — CWT FFT convolution and fast DCT/DST kinds [minor]. Closed; the delivery record is in git history.
<a id="apollo-ci-draft-prs-run-nothing"></a>- **APOLLO-CI-DRAFT-PRS-RUN-NOTHING** — Run nothing for a draft pull request [ci] [patch]. [#468](https://github.com/ryancinsight/apollo/pull/468): `ci.yml` ran the whole workflow on every push to a draft pull request; four stacked drafts on 2026-09-15 queued twenty-odd jobs ahead of the enqueued #464 and starved it for an hour. The six root jobs skip while the pull request is a draft, `ready_for_review` runs the one verification, the merge gate on main is unchanged.
<a id="atlas-apollo-caches-misc-junk-drawer"></a>- **ATLAS-APOLLO-CACHES-MISC-JUNK-DRAWER** — Split `mixed_radix/caches/misc.rs` by concern [patch]. [#470](https://github.com/ryancinsight/apollo/pull/470): `caches/misc.rs` (449 lines) splits into `radices.rs`, `factors.rs`, `pfa.rs` and `rader.rs`, bodies unchanged, the manifest re-exporting the same eight names so no caller moves; no `helpers`, `utils`, `misc` or `common` module remains in apollo-fft.
<a id="apollo-n16-register-permute"></a>- **APOLLO-N16-REGISTER-PERMUTE-2026-09-04** — The N=16 codelet's promotion gate was never the permutation [minor] [perf]. **Integrator:** claude-opus-5; **branch:** `perf/apollo-n16-register-permute`; **lease:** `components/codelet/` 2026-09-04T17:00Z.
<a id="atlas-apollo-bench-smoke-cold-compile"></a>- **ATLAS-APOLLO-BENCH-SMOKE-COLD-COMPILE-2026-08-27** — Keep compilation outside the runtime bound [ci] [patch]. Delivered in [#153](https://github.com/ryancinsight/apollo/pull/153) (c177a7ea): `ci.yml` builds the named benchmark targets in a step of its own (`Build benchmark smoke`) and runs only them inside the unchanged 60-second bound; the hosted job has run green on every main push since.
<a id="apollo-planar-lane-order-inverse"></a>- **APOLLO-PLANAR-LANE-ORDER-INVERSE** — Carry the plane column order in both directions [patch]. PR [#364](https://github.com/ryancinsight/apollo/pull/364): `LaneOrder` carries the inverse map beside the forward one; the transposes and the decimation write through it; ADR 0057 revised. The escaped defect of the seam lane order (AVX-512 eight-lane `f64`, no involution) closed; main green on the next AVX-512 landing run.
<a id="apollo-planar-seam-fusion"></a>- **APOLLO-PLANAR-SEAM-FUSION** — Delete the planar route's deinterleave and reinterleave passes [patch] [perf]. **Outcome:** [ADR 0054](docs/adr/0054-planar-seam-fusion.md) Accepted: source and sink seams ride the stage passes, 3 to 12% in the census; one-line column blocking rejected; PR #353 merged 2026-09-08.
<a id="atlas-apollo-kernel-entry-asserts"></a>- **ATLAS-APOLLO-KERNEL-ENTRY-ASSERTS** — Promote kernel-entry guards to release asserts [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-branch-inventory"></a>- **ATLAS-APOLLO-BRANCH-INVENTORY-2026-08-28** — Eight stale branches carry unmerged deltas [patch]. Closed; the delivery record is in git history.
<a id="m"></a>- **Moirai forwarding-wrapper removal [major] — done** — . [x] [major] D10-remove-moirai-forwarder (owner Codex, completed 2026-07-17; scope `apollo-fft` radix-composite execution policy and version metadata): remove the unreferenced Apollo forwarding helper, route the kernel directly to Moirai's `for_each_chunk_mut_enumerated_with`, preserve the ZST threshold policy, and advance `apollo-fft` 0.23.0 → 0.24.0. Acceptance is zero source references to the Apollo wrapper path, clean formatting, and value-semantic package tests; Nextest passes 393/393.
<a id="apollo-required-merge-checks"></a>- **APOLLO-REQUIRED-MERGE-CHECKS** — Enforce core verification before automatic merge [patch]. Installed and read back on 2026-09-06: strict `rust workspace` and `Lockfile integrity / Lockfile integrity` requirements on `main`, with automatic merge enabled. The prior unprotected branch let `--auto` merge before hosted checks completed; [landed CI](https://github.com/ryancinsight/apollo/actions/runs/34015129487) subsequently passes.
<a id="apollo-planar-transpose-folded-loads"></a>- **APOLLO-PLANAR-TRANSPOSE-FOLDED-LOADS** — Load the transpose tiles with the cross-half stage in the loads [patch] [perf] — done 2026-09-10 (rejected). **Outcome:** built on hermes `Vector::load_transposed_square` (hermes#163, since removed) and rejected on measurement: the transpose pass level on the rectangles (32768 `f32` 23.1k against 23.0k cycles, 2048 `f32` 1.1k against 1.0k) and 40 to 70% slower on the squares (4096 `f32` 2.2k against 1.3k, 16384 `f32` 9.8k against 6.5k, 65536 `f32` 41k against 30k, `f64` likewise), the bench level (`../../output/apollo-planar-rectangular/folded_*`). The assembly shows LLVM keeping each 128-bit half as a load plus a register `vinsertf128` rather than the memory-operand form, so the shuffle-port count stays at 24 per `f32` tile while the loads double; the pass sits at that floor with the intrinsic patterns available.
<a id="atlas-apollo-planar-movement"></a>- **ATLAS-APOLLO-PLANAR-MOVEMENT-2026-08-28** — What is left of the planar route's movement [perf]. Closed; the delivery record is in git history.
<a id="apollo-planar-l1-route-spike"></a>- **APOLLO-PLANAR-L1-ROUTE-SPIKE** — Decide a shorter pass structure for the L1-resident odd powers [patch] [perf] (closed with the finding). **Finding:** the planar route's floor at 2048 `f32` is structural. The attribution round put every stage within 25% of the FP-port floor, and the prototype tested that reading: the bottom pass of each stage set (stages 2 and 4, twiddles `1`, `1`, `W_4^1`) run without its three multiplies measured level at 2048 in both precisions (`t1` 1.58k/1.62k against 1.60k/1.52k cycles, `f2` 2.55k/2.59k against 2.84k/2.44k, the bench 2.3/2.2 against 2.3/2.3 µs) and above the base at 16384 `f64` in both runs (`../../output/apollo-planar-rectangular/bottom_*`), so those passes are bound by their seam shuffles and load/store traffic, not by arithmetic, and no multiply saved inside the layout moves them. The prototype is reverted. A shorter route at L1 sizes is a different kernel family (radix-16 passes over interleaved data, RustFFT's shape), filed under the parent as its own design item when taken up; ADR 0060 records the finding.
<a id="apollo-base128-level-combine-vector"></a>- **APOLLO-BASE128-LEVEL-COMBINE-VECTOR** — Vectorize the base-128 route's radix-2 level combine [patch] [perf]. PR [#367](https://github.com/ryancinsight/apollo/pull/367): `split_boundary::CombineLevel` through `vectorize`, the scalar loop the reference; 1024 `f64` 1.8 to 1.6 µs on `rustfft_comparison` (`../../output/apollo-base128-level/`). The level's remaining 763 cycles at 1024 are attributed in [the base-128 attribution](#apollo-base128-attribution).
<a id="d17-"></a>- **D17-scope-benchmark-regression-gate [patch] — done** — . Owner: Codex `/root`; scope: benchmark workflow triggering, ADR 0036, and synchronized PM records. The native measurement runtime, statistical thresholds, transform kernels, and benchmark workloads are non-goals.
<a id="atlas-apollo-split-boundary"></a>- **ATLAS-APOLLO-SPLIT-BOUNDARY-2026-08-31** — Vectorize the split's gather; fuse its combine levels [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-sweep-stops-at-512"></a>- **ATLAS-APOLLO-SWEEP-STOPS-AT-512-2026-08-31** — The comparison sweep cannot see the sizes where per-length kernels live [patch]. Closed; the delivery record is in git history.
<a id="apollo-four-step-compact-fold"></a>- **APOLLO-FOUR-STEP-COMPACT-FOLD** — Fold twiddles from a two-level table [patch] [perf]. **Outcome:** [ADR 0059](docs/adr/0059-compact-four-step-fold.md) Accepted: `m (F + m / F)` entries from `2^18`, the fold sweep at 262144 halved (`f64` 713k to 363k cycles, `f32` 222k to 142k), whole transform 6% and 3% faster there, neutral below; evidence `../../output/apollo-compact-fold/`; PR #361 merged 2026-09-09.
<a id="moirai-pool-retained-footprint"></a>- **MOIRAI-POOL-RETAINED-FOOTPRINT-2026-08-27** — Per-worker queue retention is sized by alignment padding and first-touch shape [perf]. Delivered by PR #184, PR #162, `b42ec745`, `bfeca7fc`.
<a id="atlas-apollo-flat-base-split"></a>- **ATLAS-APOLLO-FLAT-BASE-SPLIT-2026-08-28** — Gather once for the whole small-size split [perf]. Closed; the delivery record is in git history.
<a id="✅ atlas-apollo-le"></a>- **ATLAS-APOLLO-LETO-INPLACE-TYPED-ERROR** — Migrate leto-inplace panics to typed errors [major] (as [patch]). Closed; the delivery record is in git history.
<a id="d14-"></a>- **D14-align-leto-merge-pin [patch] — done** — . Owner: Codex; scope: `Cargo.lock` Leto source revisions and synchronized PM records.
<a id="apollo-nufft-3d-type2-scale"></a>- **APOLLO-NUFFT-3D-TYPE2-SCALE** — The fast 3-D NUFFT Type-2 returned the direct sum over the oversampled volume [major]. Found by the 2026-09-18 capability audit (`../../output/apollo-capability-audit-2026-09-18.md` item 1, ratio 4096 at n=4, w=6). The CPU passes run the unnormalized inverse and the WGPU load stage scales by `mx·my·mz`; `fast_type2_3d_tracks_the_direct_sum` bounds the error by `3.1e-6 Σ|f_k|` from the kernel's documented per-axis error and fails at 21 against 8.5e-5 on the old code; the WGPU test fails with the scale removed, so it runs on this host.
<a id="apollo-ntt-field-validation"></a>- **APOLLO-NTT-FIELD-VALIDATION** — Refuse NTT fields the transform cannot invert [major]. Audit item 4 (`../../output/apollo-capability-audit-2026-09-18.md`). `transform_root` owns the field contract (deterministic Miller–Rabin over twelve prime witnesses, order of `ω` exactly `n` via `ω^(n/2) ≠ 1`), shared by `NttPlan::with_modulus` and the WGPU residue validation; the circle domain rejects composites; the GPU's duplicate `mod_pow_u64` deleted. Tests: primality against trial division below 10⁴ and known strong pseudoprimes, the audit's `(4, 17, 2)` and modulus-9 plans refused, `(4, 17, 3)` against the direct sum.
<a id="apollo-mem-3d-scratch-roles-done"></a>- **APOLLO-MEM-3D-SCRATCH-ROLES** (delivered) — The 3-D chain through one scratch role [patch]. The chain alternates between the caller's storage and one role, the z pass running out of place through `lanes::execute_from` (each lane copied into place and transformed while resident), so the calling thread holds one volume, not two (`full_transform_holds_one_volume_of_caller_scratch`: 32³ f64 and 16³ f32), and the output matches the per-axis sequence bit for bit. `chain_one_role_against_two` (one binary, arms alternating, unpinned): 128³ f64 forward 7.87 to 8.12 ms against 8.83 to 9.58, inverse 7.64 to 8.20 against 8.56 to 9.16 (minima; medians 11% under); 64³ overlaps within its run spread.
<a id="apollo-n8-f64-framed"></a>- **APOLLO-N8-F64-FRAMED** — Run the register N=8 codelet inside the plan's frame [patch] [perf]. The framed `f64` entry takes `n8::vector_arm` (inlined whole); the probing entry keeps the scalar codelet, where the call into the frame loses on the efficiency core (round trip 18.5 to 21.8 ns, per-lane pass 1.02 to 1.30 ms). `f64` 8 against RustFFT: performance core 4.06 to 4.16 ns (1.14 to 1.17) to 3.10 to 3.21 (0.89 to 0.94), efficiency core 6.08 to 5.79 (0.99 to 0.91 to 0.94); a static census had the form leaner than RustFFT's (34 against 61 instructions, no spills), and the 2026-09-06 decline measured it across a call (`../../output/apollo-base128/n8_f64_framed_2026-09-17/`, `n8_f64_census_2026-09-17.md`).
<a id="apollo-flat-radix3-shared"></a>- **APOLLO-FLAT-RADIX3-SHARED** — Run the flat radix-3 pass through the shared register kernel [patch]. The flat pass's register DFT-3 and its constants deleted for `radix3` (fused outputs), `Thirds::from_scalars` serving the Winograd scalar family and one `SINE_THIRD`; `f32` 243 and 720 level on both cores, the free route at 180 142 to 181 ns before and 133 after on the performance core (`../../output/apollo-base128/flat_radix3_shared_2026-09-17/`). Every radix-3 register butterfly in apollo-fft now runs one kernel.
<a id="apollo-radix3-sign-folded"></a>- **APOLLO-RADIX3-SIGN-FOLDED** — Fold the radix-3 quarter turn's signs into its sine [patch] [perf]. `radix3` multiplies the swapped difference by `(s, -s)` or `(-s, s)` in place of a sign flip and `s` (seven operations a triple), bit-identical to the rotation form (swept test); the pair constants from the capability token ([hermes#176](https://github.com/ryancinsight/hermes/pull/176), lock advanced), since `Vector::splat_pair`'s probe per sink chunk cost `f64` 384 10%. Alternating pinned A/B against the shared sink: efficiency core `f32` 180 193 to 188 ns, 384 420 to 412 ns (1.06 to 1.05 of RustFFT), `f64` 180 322 to 310 ns, 384 740 to 731 ns; performance core `f32` 384 1.02 to 1.00, `f64` 384 level (`../../output/apollo-base128/radix3_sign_folded_2026-09-17/`).
<a id="apollo-radix3-sink-shared"></a>- **APOLLO-RADIX3-SINK-SHARED** — Run the four-lane radix-3 sink through the shared register kernel [patch]. `FinalRadix3Sink` calls `radix3` in place of its own copy (the fused form, its constant fields deleted); `f64` 384 level in alternating pinned A/B (298.8 to 301.4 ns performance core, inside the identical-code band; 738.7 to 742.2 ns efficiency core) (`../../output/apollo-base128/radix3_sink_shared_2026-09-17/`).
<a id="apollo-column-split-twiddles"></a>- **APOLLO-COLUMN-SPLIT-TWIDDLES** — Hold the column passes' twiddles split where the table stays small [patch] [perf]. `TwiddleLayout` per chain level (split up to 256 KiB, interleaved above); against the fused radix 3, alternating pinned A/B: performance core `f32` 2048 -5%, 16384 -3%, `f64` 4096 -3%; efficiency core `f32` 384 1.09 to 1.07 of RustFFT, 8192 1.01 to 0.97, 16384 1.02 to 0.99; split at every level lost 16 to 18% at 131072 and 262144 (`../../output/apollo-base128/column_twiddle_layout_2026-09-17/`, ADR 0061 note).
<a id="apollo-radix3-fused-scales"></a>- **APOLLO-RADIX3-FUSED-SCALES** — Fuse the radix-3 register kernel's scales into its adds [patch] [perf]. RustFFT's `column_butterfly3` form, eight operations a register triple against ten; alternating pinned A/B, apollo over RustFFT: `f32` 180 1.03 to 0.94 (performance) and 0.98 to 0.94 (efficiency), `f64` 180 0.99 to 0.87 / 0.92, `f32` 384 1.03 / 1.06 to 1.02 on the performance core and 1.10 unchanged on the efficiency core, 100 unchanged (`../../output/apollo-base128/radix3_fused_2026-09-17/`).
<a id="apollo-384-as-three-128-blocks"></a>- **APOLLO-384-AS-THREE-128-BLOCKS** — Run 384 as three 128-blocks under a radix-3 step over the base [minor] [perf]. [#429](https://github.com/ryancinsight/apollo/pull/429), [#430](https://github.com/ryancinsight/apollo/pull/430): the three-block source at both widths and the radix-3 sink over the 128 base; `f64` 384 0.99 of RustFFT from 1.26, `f32` 1.19 from 1.37 (`../../output/apollo-base128/small_sizes_384*_2026-09-11.txt`); ADR 0061 revised. The `f32` residue is the four-window source against a gather.
<a id="apollo-peak-independent-reference"></a>- **APOLLO-PEAK-INDEPENDENT-REFERENCE** — Check peak estimates against direct sums [patch]. **Delivered:** direct-DFT and geometric-reference checks of every resolvable read, the inverse-tangent closure and index-based DC/Nyquist rejection, tie-broken subtraction order; joined [PR #494](https://github.com/ryancinsight/apollo/pull/494).
<a id="atlas-apollo-n32768-f64-variance"></a>- **ATLAS-APOLLO-N32768-F64-VARIANCE-2026-08-31** — Exact-processor f64 N=32,768 latency retains a 22.3% band [patch] [perf]. Delivered by PR #217, `3d3a7d37`, `5bf93047`.
<a id="atlas-apollo-radix-tables-disagree"></a>- **ATLAS-APOLLO-RADIX-TABLES-DISAGREE-2026-09-02** — Two radix tables disagree about order, and each is faster somewhere [patch] [perf]. **Finding.** Apollo carries two sources for the same decision — which order to apply the radices of a composite length — and they disagree in both directions, by tens of percent. Measured in one binary, plan against free dispatcher, minimum of 100 pinned samples: | n | plan's order | dispatcher's order | faster | | --- | --- | --- | --- | | 100 | `[5, 5, 4]` (was) | `[4, 5, 5]` | dispatcher by 29% | | 384 | `[3, 4, 4, 4, 2]` (was) | `[4, 4, 4, 2, 3]` | dispatcher by 26% | | 180 | `[5, 3, 3, 4]` | `[4, 3, 3, 5]` | **plan by 62%** | The first two are fixed (`#atlas-apollo-plan-underselects-composite`) by having the plan read the dispatcher's table. n = 180 is the counter-example that says the table is not simply right: its entry is 2.6x slower than the order the plan hand-lists, and the free dispatcher still uses it. The two-dimensional lane passes take the free route for non-power-of-two axes, so a 180-length axis pays it per line.
<a id="apollo-384-f32-gather"></a>- **APOLLO-384-F32-GATHER** — 384 as three 128-blocks under a radix-3 pass ahead of them at eight lanes [patch] [perf]. [#445](https://github.com/ryancinsight/apollo/pull/445): the column-first kernels generic over the radix, 384 at eight lanes as the radix-3 pass over the parent, three 128-blocks into scratch and one three-way interleave back; `f32` 384 1.02 / 1.06 of RustFFT on the performance core from 1.10 and 1.06 on the efficiency core from 1.16 to 1.17 (9% under the three-block sink form on both), no other length slower (`../../output/apollo-base128/small_sizes_column384_run*_2026-09-14.txt`); ADR 0061 note. Residue: the 128-block at eight lanes (`f32` 128 0.92 to 0.95).
<a id="apollo-n16-f64-gap"></a>- **APOLLO-N16-F64-GAP-2026-09-04** — RustFFT is 18% faster at N=16 f64 [minor] [perf]. **Integrator:** claude-opus-5; **branch:** `perf/apollo-n16-f64-gap`; **lease:** the N = 16 f64 route and its codelets 2026-09-04T19:30Z.
<a id="apollo-composite-ragged-rows"></a>- **APOLLO-COMPOSITE-RAGGED-ROWS** — Take the register-granularity waste out of odd rows [patch] [perf]. PR [#397](https://github.com/ryancinsight/apollo/pull/397): the run-over tail's twiddle registers load once per stage (per stage against the AVX2 kernels, `f64` rows of three 1.28 to 1.15, `f32` 0.96 to 0.74); whole transforms `f32` 3-chains 0.96 to 1.04 and `f64` 1.02 to 1.10 (`../../output/apollo-composite-generic/orders16_*`).
<a id="apollo-ci-ready-for-review-runs"></a>- **APOLLO-CI-READY-FOR-REVIEW-RUNS** — Run the pull-request verification when a draft is marked ready [ci] [patch]. [#475](https://github.com/ryancinsight/apollo/pull/475): `ready_for_review` is not in the default `pull_request` event set and the #468 draft filter skips every job of a draft push, so #469 marked ready at 12:29 sat BLOCKED with every check SKIPPED; the trigger names its event set with `ready_for_review` added, the draft filter unchanged.
<a id="atlas-apollo-padded-stride"></a>- **ATLAS-APOLLO-PADDED-STRIDE-2026-08-26** — Pad the batched plane stride [patch]. Closed; the delivery record is in git history.
<a id="apollo-planar-source-placement"></a>- **APOLLO-PLANAR-SOURCE-PLACEMENT** — Start the planar planes on a cache line [patch] [perf]. PR [#369](https://github.com/ryancinsight/apollo/pull/369): the swing was sixteen-byte allocator alignment splitting register loads across lines, not page placement; caller-lent scratch now starts its planes on a line with slack (`../../output/apollo-planar-rectangular/align_*`). The plan's own scratch already came from mnemosyne on a line.
<a id="atlas-apollo-base-memset"></a>- **ATLAS-APOLLO-BASE-MEMSET-2026-08-29** — Fold the bounds checks; drop the dead zero-fill [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-mellin-real-complex-dot"></a>- **ATLAS-APOLLO-MELLIN-REAL-COMPLEX-DOT-2026-09-01** — Remove forward-spectrum lane materialization [patch] [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-f32-n32768-throughput"></a>- **ATLAS-APOLLO-F32-N32768-THROUGHPUT-2026-08-31** — Localize the stable f32 N=32,768 deficit [patch] [perf]. **Delivered:** source `5a1b3e8a`, PR #218, merge `23993c6a`; native eight-lane f32 transpose reduces the phase from 50,454--50,859 to 17,341--17,564 cycles and the unchanged whole-plan median from 82.970 to 72.145--74.005 microseconds.
<a id="apollo-stranded-branches"></a>- **APOLLO-STRANDED-BRANCHES-2026-09-04** — Two abandoned branches hold deltas main does not [patch]. **Outcome:** both deleted, no unique work. `origin/test/apollo-stft-leto-sidecar` had by 2026-09-10 become the pushed home of the local `chore/apollo-mnemosyne-rev-and-f16-gate` lineage (its eight commits, last `f4537f26`), and its delta against main was a mnemosyne `rev = "e26ee02"` pin (quarantine main never adopted), the removal of the `half` workspace dependency main carries, a stripped Cargo.lock, and no source change: the `AlignedVec` staging migration and the validation-binary allocator wiring named on 2026-09-04 are not on it. The local branch exists in no tree or lane. apollo's local branch list maps to open items and enqueued PRs only.
<a id="apollo-rotated-order-handoff"></a>- **APOLLO-ROTATED-ORDER-HANDOFF-2026-09-10** — The third move of a 3-D transform only restores the caller's layout [minor] [perf]. **Integrator:** claude-opus-5; **branch:** `perf/apollo-rotated-order-handoff`.
<a id="apollo-chain-pass-spills"></a>- **APOLLO-CHAIN-PASS-SPILLS** — Cut the radix-8 column pass's spill traffic [patch] [perf]. [#457](https://github.com/ryancinsight/apollo/pull/457): two trials (pairwise output consumption; half-root-two eighths) — the baseline eight-lane loop 143 instructions a chunk, 114 vector, 27 loads and 9 stores of which 11 are spill moves, critical path 37 cycles against an issue bound of 29 to 38; trial one (pairwise consumption) 141 / 112 vector, 9 spill moves, critical path 44; trial two (pairwise and half-root-two eighths) 133 / 104 vector, 22 loads and 8 stores of which 6 are spill moves, 27 shuffles, critical path 45 against 26 to 35. `f32` 131072 +5% / -1%; `f32` 32768 -5% / +2%; `f32` 16384 +0% / -1%; `f64` 131072 -2% / +2%; `f64` 32768 -1% / -0% in the phase meter; Not retained: the spill traffic is not on the critical path (its loads and stores overlap the arithmetic), and the shorter instruction stream of trial two comes with a longer dependency chain (critical path 37 to 45 cycles against an issue bound of 26 to 35), so the cache-resident innermost passes slowed 2 to 5% on both cores while the outer levels stayed inside their noise. The efficiency core charges the chain for its 256-bit instruction count, but a cut that lengthens the chain past the issue bound trades one bound for the other; no candidate cuts instructions without lengthening the chain, so `f32` 131072 stays on the four-step at eight lanes and the branch carries the readings and the trial patch (`output/apollo-base128/chain_pass_spill_trials_2026-09-15.patch`), not the code. (`../../output/apollo-base128/chain_phases_spills_2026-09-15.txt`); ADR 0061 note.
<a id="atlas-apollo-bench-smoke-runtime"></a>- **ATLAS-APOLLO-BENCH-SMOKE-RUNTIME-2026-08-27** — Separate bounded smoke from local timing [patch]. Closed; the delivery record is in git history.
<a id="apollo-pstd-consumer-transform-weight"></a>- **APOLLO-PSTD-CONSUMER-TRANSFORM-WEIGHT-2026-09-15** — The 64 cubed real pair is most of a consumer step [patch] [perf]. **Apollo's census, 2026-09-16.** `real_volume_arms_against_the_consumer` times both entries singly and in the consumer's two compositions at 64 cubed f64, machine-wide over two runs agreeing within 3% (`output/apollo-base128/real_volume_arms_2026-09-16.md`): forward 178-183 us, inverse 210 us, the velocity composition (one forward, three inverses) 843-864 us, the density composition (three of each) 1235-1252 us. The route is the half-spectrum pair, `forward_3d_half_into` and `inverse_3d_half_into`, which is what the consumer calls; no arm takes its staging branch.
<a id="apollo-stockham-module-allows"></a>- **APOLLO-STOCKHAM-MODULE-ALLOWS** — Retire the module-scoped lint allows in the Stockham kernels [patch]. [#473](https://github.com/ryancinsight/apollo/pull/473): all ten module-scoped allows in apollo-fft retired, not only the two Stockham files — seven dead against the crate-level list, `type_complexity` closed by the `SharedTable`/`LocalTable` aliases in `caches/tables.rs` and `PfaPermutation`, `same_item_push` by `repeat_n`, an unused test import deleted, and the one live `uninit_vec` (the twiddle table built into spare capacity) under a per-site `#[expect]` with its reason; the crate-level list in `lib.rs` stays as the documented set.
<a id="atlas-apollo-census-unpinned-blend"></a>- **ATLAS-APOLLO-CENSUS-UNPINNED-BLEND-2026-09-01** — The engine census measures an unpinned two-class blend [minor] [perf] — done 2026-09-01 (merged 12b9c4d4). Delivered by `f0332d9d`.
<a id="APOLLO-WASM-DEPENDENCY-2026-09-10"></a>- **APOLLO-WASM-DEPENDENCY-2026-09-10** — Keep the FFT dependency graph portable on WebAssembly [patch]. Landed as [#386](https://github.com/ryancinsight/apollo/pull/386) (cefd55ec): the unused production `rand` edge is gone from `apollo-fft`, so `getrandom` 0.4 leaves the wasm32 graph; the consumer side closed with Coeus #393 (merged 2026-09-10). Verified 2026-09-15 against origin/main: no `rand` dependency in the crate manifest.
<a id="apollo-base128-level-in-sink"></a>- **APOLLO-BASE128-LEVEL-IN-SINK** — Fold the 1024 combine level into the second half's final sink [patch] [perf]. Delivered by the four-block radix-4 sink (`c4b3f1f7`, [the two-pass base](#apollo-l1-base-two-pass) slice 10): the level runs inside the fourth block's store, the meter reads `levels=0` at 1024 (`../../output/apollo-base128/small_sizes_base512_run*_2026-09-11.txt`), 1024 `f64` 1.00 to 1.09 and `f32` 0.96 to 1.05 of RustFFT.
<a id="atlas-apollo-inverted-core-claims"></a>- **ATLAS-APOLLO-INVERTED-CORE-CLAIMS-2026-09-01** — Sweep merged claims resting on the swapped core labels [patch]. Delivered by `b21fc172`.
<a id="atlas-apollo-pot-throughput"></a>- **ATLAS-APOLLO-POT-THROUGHPUT-2026-08-25** — Profile the power-of-two f64 path [patch]. Closed; the delivery record is in git history.
<a id="apollo-2048-as-four-512-blocks"></a>- **APOLLO-2048-AS-FOUR-512-BLOCKS** — Run 2048 as four sixteen-row blocks under the radix-4 sink [minor] [perf]. [#425](https://github.com/ryancinsight/apollo/pull/425): the radix step generic over the row count, 2048 as four 512-blocks ahead of the four-step; `f64` 2048 1.24 / 1.11 of RustFFT from 1.38 to 1.40, `f32` 1.29 from 1.62 (`../../output/apollo-base128/small_sizes_2048_run*_2026-09-11.txt`); ADR 0061 revised. The remainder is the radix-4 sink over four 512-spectra.
<a id="atlas-apollo-odd-power-fusion"></a>- **ATLAS-APOLLO-ODD-POWER-FUSION-2026-08-28** — Fuse the radix-2 decimation into the planar boundary [perf]. Closed; the delivery record is in git history.
<a id="apollo-board-completed-section-union"></a>- **APOLLO-BOARD-COMPLETED-SECTION-UNION** — Make the board's completed tail merge without a hand resolution [patch]. **Delivered:** the compactor orders the done section by the SHA-256 of each anchor, so a record inserts at its own place instead of the shared last line; a `git merge-file` rehearsal over the board's 276 entries conflicted on 275 of 275 consecutively recorded pairs appended at the end and on 3 in hashed order, and a unit test merges two concurrent records cleanly where the appended form conflicts. The filed `merge=union` sibling was not built: it rests on the hosting honouring a gitattributes driver, which is unverified, and it would break every `backlog.md#<id>` link to a done item. The compactor also now fails on duplicate anchors and dangling in-file links. Remaining conflict forms: adjacent hashed insertions, and adjacent open blocks recorded concurrently.
<a id="atlas-apollo-first-party-layout"></a>- **ATLAS-APOLLO-FIRST-PARTY-LAYOUT-2026-09-03** — Apollo GPU layouts use Eunomia [patch] [arch] — done . **Status:** Merged in PR #315 at `07e2eb54`; Apollo owns no direct `bytemuck` or `half` dependency and uses Eunomia for GPU layout contracts.
<a id="apollo-lane-task-width"></a>- **APOLLO-LANE-TASK-WIDTH-2026-09-09** — A 3-D lane pass scheduled one lane per task, and ran slower than serial [patch] [perf]. **Integrator:** claude-fable-5.1; **branch:** `perf/apollo-lane-task-width`, authored through the git API — both apollo trees were held by peers.
<a id="atlas-apollo-odd-power-routing"></a>- **ATLAS-APOLLO-ODD-POWER-ROUTING-2026-08-28** — Odd powers of two took the slow route [patch]. Closed; the delivery record is in git history.
<a id="apollo-codegen-clause-evidence"></a>- **APOLLO-CODEGEN-CLAUSE-EVIDENCE** — Acceptance clauses about emitted code now have an instrument [patch]. Delivered: `scripts/codegen_attribution.py` `compare` (per-symbol instruction deltas, byte identity of the bodies) and `loops` ([#453](https://github.com/ryancinsight/apollo/pull/453): loop census, spill traffic, critical path). The open half is a rule rather than a queue: an item carrying a codegen, size or scratch clause attaches the attribution output when it is next worked.
<a id="apollo-semver-locked-inputs"></a>- **APOLLO-SEMVER-LOCKED-INPUTS** — Compare APIs without provider re-resolution [patch]. Closed 2026-09-15 on the shared gate: `ci.yml` consumes atlas `semver-gate.yml@806949a`, which runs `cargo-semver-checks-action` against the pull request's base revision with both checkouts' committed locks (`fetch-depth: 0`), so no provider is re-resolved; the `^0.5.0` failure belonged to the stripped-lock era the lockfile guard closed. The action reports the two revisions and the toolchain and distinguishes a build failure from a detected break; the informational job ran green on #459 through #472 and the release gate runs only under the release workflow.
<a id="apollo-sweep-exceeds-budget"></a>- **APOLLO-SWEEP-EXCEEDS-BUDGET-2026-09-04** — The reference sweep is terminated at the test budget [patch] [perf]. **Delivered.** The sweep completes in 23.8 s, inside both the 30 s slow mark and the 60 s termination bound; it was terminated at 60 s before.
<a id="atlas-apollo-small-size-falsified"></a>- **ATLAS-APOLLO-SMALL-SIZE-FALSIFIED-2026-08-28** — Two small-size hypotheses, both falsified [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-wavelet-doc-links"></a>- **ATLAS-APOLLO-WAVELET-DOC-LINKS-2026-08-29** — Three unresolved intra-doc links in apollo-wavelet [patch]. Delivered by `e2f7ed21`.
<a id="apollo-3d-attribution-module"></a>- **APOLLO-3D-ATTRIBUTION-MODULE** — Keep attribution reporting in a leaf module [patch]. **Outcome:** Windows-only processor and axis attribution reporting now lives in `dimension_3d/pass_attribution/report.rs`; the parent module is 355 lines and the `apollo-fft` suite remains green (622 passed, 42 skipped).
<a id="apollo-planar-seam-lane-order"></a>- **APOLLO-PLANAR-SEAM-LANE-ORDER** — Store plane columns in the unpack lane order [minor] [perf]. **Outcome:** [ADR 0057](docs/adr/0057-planar-columns-in-sublane-order.md) Accepted: planes in the dispatched backend's sub-lane order, seams reduced to the unpacks, one selector for every planar kernel; neutral at 65536, and the pairs exposed the sink sweep's placement lottery, filed as [seam staging](#apollo-planar-seam-staging); PR #357 merged 2026-09-09.
<a id="d20-"></a>- **D20-real-spherical-harmonic-provider [minor]**. Delivered: `apollo-sht` real even-order spherical-harmonic evaluation and scattered-direction design matrices, provider default `db218665` (an ancestor of main); RITK consumes the real basis at `53bb0131`. Residual: the SemVer comparison stays blocked by dependency rustdoc failures (`naga`, `apollo-fft-macros`, `hermes-simd-intrinsics`).
<a id="apollo-base128-probe-module"></a>- **APOLLO-BASE128-PROBE-MODULE** — Keep measurement helpers in the probe module [patch]. **Outcome:** Windows-only incumbent and four-block measurement helpers now live under `base128/pinned_probe`; `base128/mod.rs` is 451 lines and the `apollo-fft` suite remains green (622 passed, 42 skipped).
<a id="apollo-probe-scale-disagreement"></a>- **APOLLO-PROBE-SCALE-DISAGREEMENT-2026-09-04** — Two pinned probes disagree by 25x on one transform and reverse its verdict [patch] [perf] (retracted). **Retracted: there was no disagreement.** The two readings came from different build profiles. The codelet probe was run without `--release` and the small-sizes sweep with it; debug reads apollo's N = 16 route at 242 ns and RustFFT at 610 ns, release reads 10.6 ns and 9.0 ns. Nothing about the instruments differed.
<a id="atlas-apollo-core-class-upstream"></a>- **ATLAS-APOLLO-CORE-CLASS-UPSTREAM-2026-09-01** — Consume themis core efficiency class [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-combine-sink"></a>- **ATLAS-APOLLO-COMBINE-SINK-2026-08-31** — The split's combine rides the column pass out [perf]. Closed; the delivery record is in git history.
<a id="apollo-board-compaction"></a>- **APOLLO-BOARD-COMPACTION** — Compact the board to its line budget [patch]. [#455](https://github.com/ryancinsight/apollo/pull/455): `scripts/compact_board.py` (unit-tested) takes the board from 6981 lines to 757 — every item anchored (125 anchors to 276, none lost), statuses in the closed set, open items at most fifteen lines, done items one line each in the closing section, a hundred sprint sections dropped, one nine-day-stale claim released; the pre-compaction board is the PR's base commit.
<a id="apollo-board-compaction-forms"></a>- **APOLLO-BOARD-COMPACTION-FORMS** — The first compaction misread the legacy heading forms [patch]. [#458](https://github.com/ryancinsight/apollo/pull/458): twenty-five legacy headings ("done (rejected)", "measured: ...", "in progress", "complete", "blocked: ...") had been read as statusless and given a second todo, and nine inline anchors kept beside derived ones; `scripts/compact_board.py` parses every status segment (note kept beside the title), reads inline anchors as the item, passes its own done entries through (a second run changes nothing), unit-tested per form; the board 758 to 543 lines, 277 anchors kept.
<a id="apollo-benchmark-baseline-closure"></a>- **APOLLO-BENCHMARK-BASELINE-CLOSURE** — Preserve benchmark dependency closures [patch]. `b944eef5` preserves each revision's production files and lock while checking shared instrument sources; ten filesystem tests pass. Exact `09325122` baseline and `7013ea96` candidate compile all three unchanged release targets under separate original locks and enforced 600-second build bounds: [baseline](../../output/apollo-square-transpose/integration/baseline-preparation/baseline-build-collection.json), [candidate](../../output/apollo-square-transpose/integration/merge-gates/collection.json). No timing or transitive machine-code equality claim.
<a id="atlas-apollo-batched-native-width"></a>- **ATLAS-APOLLO-BATCHED-NATIVE-WIDTH-2026-09-01** — The f32 reinterleave sink ran at half the native width [patch] [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-real-split-coverage"></a>- **ATLAS-APOLLO-REAL-SPLIT-COVERAGE-2026-08-25** — Extend the split past the 1-D forward slice [patch]. [#469](https://github.com/ryancinsight/apollo/pull/469): the full-spectrum 1-D inverses (`ifft_1d_slice`, `_array`, `_array_into`, `_array_into_spectrum_scratch`, `_leto`) read only the lower half where the split admits the length, bit-identical to the half inverse for f64/f32/F16, caller-owned forms allocation-free; census against the widened route 1.07x at 1024, 1.27x at 4096, 1.35x at 16384, 3.7x at 65536, 1.9x at 262144. The forward paths took the split earlier and the 3-D real paths have their half APIs. Residual, filed separately when picked up: the static-length variants need a plan provider bound ([major]), and a 2-D real half API does not exist yet ([minor]).
<a id="perf-f32-small-prime-001"></a>- **PERF-F32-SMALL-PRIME-001** — `f32` slower than `f64` on the Rader path [patch] (by takeover). Closed; the delivery record is in git history.
<a id="atlas-orphan-modules-096-apollo"></a>- **ATLAS-ORPHAN-MODULES-096-APOLLO** — Remove the dead large-composite leaf [patch]. Closed; the delivery record is in git history.
<a id="apollo-real-split-twiddles"></a>- **APOLLO-REAL-SPLIT-TWIDDLES-2026-09-11** — The real split recomputes its twiddles per lane [patch] [perf]. a07d0a56: untangle and retangle take their twiddles from an iterator; `FftPlan3D` keeps the table for its lanes. Quiet crossover runs, complex arms flat as control: real pair 9.9 to 8.2 µs at 16³, 36.1 to 31.4 at 24³, 40.9 to 37.1–38.3 at 32×32×16; 32³ and 48³ within noise.
<a id="apollo-composite-three-arm-interleave"></a>- **APOLLO-COMPOSITE-THREE-ARM-INTERLEAVE** — Exact first-stage scatter of three arms as a hermes three-way pair interleave [patch] [perf]. hermes [#171](https://github.com/ryancinsight/hermes/pull/171) (`interleave_pairs3`, [`hermes-interleave-pairs3`](../hermes/backlog.md#hermes-interleave-pairs3)) and the apollo slice: `store_arms::<3>` stores three whole registers through it at every width, no padded tile, no spill. Per stage against the AVX2 kernel (`../../output/apollo-composite-generic/stage_timing/sy_*`): the radix-3 first stage `f32` 1.32 to 1.09, `f64` 1.00.
<a id="atlas-apollo-half-staging-uninit"></a>- **ATLAS-APOLLO-HALF-STAGING-UNINIT-2026-09-02** — The half staging buffer was zeroed before being overwritten [patch] [perf]. **Delivered** (`gap_audit.md#half-fusion-blocked-upstream`): the register-resident storage lengths stage into `MaybeUninit` rather than a zeroed array — the widening fills every lane of it, so the zeroing was a whole extra pass over the buffer. Debug builds poison the staging with NaN so an unwritten lane fails the value oracles instead of passing quietly.
<a id="atlas-apollo-stft-window-interleave"></a>- **ATLAS-APOLLO-STFT-WINDOW-INTERLEAVE-2026-09-01** — Measure forward-frame fusion [patch] [perf] — re-adjudicated 2026-09-01: fusion adopted under the memory criterion. **Re-adjudication (2026-09-01, Claude session 03d80d33).** The rejection below applied a predeclared *latency-improvement* gate. The stack's current directive is memory efficiency, under which the acceptance is: latency not regressed, retained per-worker scratch reduced, values identical. Rebuilt the candidate rather than reviving the deleted source: interior frames (every frame wholly inside the signal) now call the provider's fused `real_mul_to_interleaved_complex` straight from the signal slice into the complex output viewed as its `2n` real lanes (`Complex` is `repr(C)`); only the frames overhanging either end take the scalar path. Both forward real pools are deleted; the one real-lane pool that remains serves the inverse path and is named for it.
<a id="atlas-apollo-hermes-complex-transpose"></a>- **ATLAS-APOLLO-HERMES-COMPLEX-TRANSPOSE-2026-09-01** — Tile multidimensional complex transposes [patch] [perf]. Delivered by PR #135, `9ac833cd`, `060eb7eb`.
<a id="apollo-real-3d-x-last-scatter"></a>- **APOLLO-REAL-3D-X-LAST-SCATTER** — Bring the x-last z sweeps to the contiguous sweep's cost [patch] [perf]. **Floor recorded:** interleaved at 64 cubed, the x-last forward sweep is 37.3-38.4 us against 21.8-25.0 contiguous; reads in x-last order cost 2.5 us of it (`read c order` / `read x-last` arms), and writing each slab front to back from a per-task block measured 35.7-38.3 against 37.3-38.4 for the lane-wise scatter, so write order is not the excess; not adopted (`output/apollo-base128/block_write_48781cc3.patch`). A slab-sized block borrowed through a released scratch role cost 144-174 us, now noted in `plan_scratch.rs`.
<a id="apollo-native-real-3d"></a>- **APOLLO-NATIVE-REAL-3D-2026-09-11** — A real 3-D field is transformed as if it were complex [minor] [perf]. a8cd1c94: `fft_3d_array_half_into`/`ifft_3d_array_half_into` and the per-lane real inverse; oracles in `tests/real_half_api/`. Fastest sample per transform at 64³ 205–210 µs against the complex pair's 271 (two probe rounds, 9–39% host load).
<a id="apollo-worker-workspace-lifetime"></a>- **APOLLO-WORKER-WORKSPACE-LIFETIME** — Bound scratch across transform submissions [patch] [arch]. Merged [PR 333](https://github.com/ryancinsight/apollo/pull/333), `b5ea7394`; five warm multidimensional cases allocate zero bytes. [ADR 0048](docs/adr/0048-worker-scratch-lifetime.md) records ownership, numerical and memory evidence, and instrumentation limits.
<a id="atlas-apollo-batched-pot"></a>- **ATLAS-APOLLO-BATCHED-POT-2026-08-25** — Power-of-two on the batched layout [arch] — merged 2026-08-25 (PR #119); 1-D reach resolved 2026-08-26. **Correction and resolution:** PR #119's path was initially live only for 2-D/3-D lane transforms. `ATLAS-APOLLO-BATCHED-1D-UNREACHABLE-2026-08-26` now routes standalone square powers through the shared four-step selector at the measured 65536 crossover and records the end-to-end census.
<a id="atlas-apollo-column-pass-consolidation"></a>- **ATLAS-APOLLO-COLUMN-PASS-CONSOLIDATION-2026-09-01** — One column pass at every width, sinks included [patch] [perf]. Closed; the delivery record is in git history.
<a id="apollo-planar-sink-row-copy"></a>- **APOLLO-PLANAR-SINK-ROW-COPY** — Copy the staged sink rows register-wide [patch] [perf] (rejected). **Outcome:** the staged rows copied with register loads and stores in place of one memcpy per row: no measurable effect. On a quiet host the sink sweep of the unchanged tree moves by up to 20% between its own two runs at 2048 to 16384 (`f64` 2048 5.1k and 4.2k cycles), the candidate sits inside that spread (4.4k and 4.8k), 4096 `f64` reads above it, and `rustfft_comparison` is flat at 2048 in both precisions; a 256-byte memcpy is not the cost. Evidence `../../output/apollo-planar-rectangular/` (`rowcopy_*`, `sections_*_r*`). The sink sweep's cost per element stays open on the parent item; the next instrument is the seam sweep measured alone against a plain sweep of the same stages, since the section probe's run-to-run spread now exceeds the effects left.
<a id="atlas-apollo-sixteen-block-split"></a>- **ATLAS-APOLLO-SIXTEEN-BLOCK-SPLIT-2026-09-03** — n = 2048 is a local peak, and the split does not reach it [minor] [perf] (falsified). **Why it looked promising.** n = 2048 measures worse than both its neighbours and is the only power of two where apollo loses to PhastFT as well — the same signature n = 1024 had before the eight-block split: | n | f64 vs RustFFT | f32 vs RustFFT | f32 vs PhastFT | | --- | --- | --- | --- | | 1024 (split) | +25% | +52% | +13% | | **2048 (untuned)** | **+39%** | **+103%** | **+33%** | | 4096 | +20% | +38% | +6% |
<a id="atlas-apollo-pot-pass-reduction"></a>- **ATLAS-APOLLO-POT-PASS-REDUCTION-2026-08-25** — Cut the pass count over the data [arch] (premise false). **Outcome:** the power-of-two f64 transform moves less memory, which is the binding constraint. Target is the pass count, not the arithmetic.
<a id="apollo-base-table-alignment"></a>- **APOLLO-BASE-TABLE-ALIGNMENT** — Align the base plan tables and the staging buffer to the cache line [patch] [perf]. [#423](https://github.com/ryancinsight/apollo/pull/423): the plan table in a 64-byte aligned wrapper, the sink tables at the first boundary inside their buffer, the staging buffer in the wrapper; the plan-level and bare 128 arms within 1% and the run-to-run band from 8% to 1.5% at every length (`../../output/apollo-base128/small_sizes_aligned_run*_2026-09-11.txt`); ADR 0061 revised.
<a id="apollo-rotated-move-geometry"></a>- **APOLLO-ROTATED-MOVE-GEOMETRY-2026-09-10** — Attribute tall moves to task partition [patch] [perf]. Delivery: `8b709811`, integrated at `5c725de7`; [Leto PR 192](https://github.com/ryancinsight/leto/pull/192) changes transpose task width only. Destination-stride traversal and FFT lane scheduling remain unchanged.
<a id="atlas-apollo-probe-first-run-artifact"></a>- **ATLAS-APOLLO-PROBE-FIRST-RUN-ARTIFACT-2026-09-02** — A freshly built probe binary's first run is not measurable [patch] [ci]. **Finding** (`gap_audit.md#first-run-after-build`): on this host the first execution of a newly linked test binary runs several times slower throughout the process — the same call read 482 ns on the run after the build and 36 ns on the next run of the same binary, with no code change. It is per-process, not per-case: an arm repeated at the end of a run reproduces the arm at its start, and two runs of an unchanged binary agree to ~2%.
<a id="atlas-apollo-hardware-lane-link"></a>- **ATLAS-APOLLO-HARDWARE-LANE-LINK-2026-08-31** — Remove the redundant portable combine specialization [patch] [perf]. Delivered by PR #110, `363c407d`.
<a id="atlas-apollo-half-storage-bulk-bridge"></a>- **ATLAS-APOLLO-HALF-STORAGE-BULK-BRIDGE-2026-09-02** — Half-storage promotion ran one lane at a time [patch] [perf]. Closed; the delivery record is in git history. Its recorded n >= 64 figures were measured one run after each build and understate the gain: corrected to a uniform -73 to -85% in `gap_audit.md#half-storage-bulk-bridge-corrected` (see `#first-run-after-build`).
<a id="atlas-apollo-engine-census"></a>- **ATLAS-APOLLO-ENGINE-CENSUS-2026-08-25** — Commit the four-engine census as an instrument [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-real-half-throughput"></a>- **ATLAS-APOLLO-REAL-HALF-THROUGHPUT-2026-09-01** — Restart real-split twiddles [patch] [perf]. Delivered by `5a8b90d3`.
<a id="apollo-real-1d-owned-forward-write-once"></a>- **APOLLO-REAL-1D-OWNED-FORWARD-WRITE-ONCE** — The owned 1-D real forwards fill their spectrum and write it twice [patch] [perf]. [#477](https://github.com/ryancinsight/apollo/pull/477) (draft, stacked on #476), finding only: the write-once route was built and measured at parity with the zero-fill-and-mirror it replaced (the 1-D form already writes at most twice, so the fill trades for a staging round trip of the same bytes), and the first probe's 0.84x / 0.73x was allocator state — a second run read 0.96x / 1.18x with identical-code arms 25% apart. Not retained; the owned forms are pinned bit-identical to the half form in one allocation and the census `real_full_forward_f64` row gains the widened arm (`engine_census_1d_owned_2026-09-15.txt`, widened over routed by the minimum: 1024 0.92x; 4096 1.11x; 16384 1.29x; 65536 1.20x; 262144 1.28x).
<a id="atlas-apollo-f64-base128-attribution"></a>- **ATLAS-APOLLO-F64-BASE128-ATTRIBUTION-2026-08-31** — Re-establish the f64 256/512 bottleneck [patch] [perf]. **Delivered:** PM-only measured rejection; exact quarter-turn twiddle reuse regressed f64 N = 512, so production source remains byte-identical to merged PR #219 / `8ef1e5d3`.
<a id="perf-pot-large-discontinuity-001"></a>- **PERF-POT-LARGE-DISCONTINUITY-001** — Power-of-two sizes ≥128 look disproportionately slow [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-planar-attribution"></a>- **ATLAS-APOLLO-PLANAR-ATTRIBUTION-2026-08-28** — Make the planar route's passes measurable [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-wide-strided-loads"></a>- **ATLAS-APOLLO-WIDE-STRIDED-LOADS-2026-09-01** — Strided phase-one loads at eight lanes [patch] [perf]. Closed; the delivery record is in git history.
<a id="apollo-codelet-schedule-controls"></a>- **APOLLO-CODELET-SCHEDULE-CONTROLS** — Share composite schedule controls [major] [arch]. Landed fc34ca11 (ADR 0051 Accepted; judge fixes 480eb862): sealed zero-sized schedules share one arithmetic body; selected, forced-fused and forced-split values agree at 50 and 144 for both precisions and directions; the size clause measured at delta 0 with byte-identical bodies for all four monomorphizations. Residual: [APOLLO-MIRI-SCHEDULE-SPLIT-COVERAGE](#apollo-miri-schedule-split-coverage).
<a id="apollo-n8-regime-split"></a>- **APOLLO-N8-REGIME-SPLIT-2026-09-06** — Superseded for the plan by [the framed arm](#apollo-n8-f64-framed), 2026-09-17. The N=8 arm decision inverts between latency and throughput [minor] [perf] (real, and not at a size consumers run). **Finding (unchanged).** [`#apollo-n8-f64-gap`](#apollo-n8-f64-gap) declined the N = 8 register codelet on a round trip, where each inverse waits on its own forward — a *latency* reading. Across 32 independent lanes the verdict inverts on the performance core: 229.3 ns scalar against 122.6 vector, a **1.87x** win, where at latency the scalar codelet leads by 1.15x. The efficiency core does not follow (1.11x to scalar at throughput), and there is no per-core dispatch.
<a id="atlas-apollo-leto-view-layout"></a>- **ATLAS-APOLLO-LETO-VIEW-LAYOUT-2026-08-27** — Preserve logical multidimensional view order [patch] [arch]. Delivered by PR #139, `61c5abdc`.
<a id="atlas-apollo-base-split-twiddle-reuse"></a>- **ATLAS-APOLLO-BASE-SPLIT-TWIDDLE-REUSE-2026-08-31** — Retain selected split twiddles in the plan [patch] [perf]. Closed; the delivery record is in git history.
<a id="apollo-prepush-clippy-flags"></a>- **APOLLO-PREPUSH-CLIPPY-FLAGS** — The pre-push gate runs clippy without the CI flags [patch] [ci]. [#426](https://github.com/ryancinsight/apollo/pull/426): the hook runs `cargo clippy --all-targets --all-features -- -D warnings` as CI does; an unused import that passed the bare form on 2026-09-11 fails it, main passes it.
<a id="apollo-2048-column-first"></a>- **APOLLO-2048-COLUMN-FIRST** — The 2048 route as a radix-8 pass ahead of the blocks [minor] [perf]. [#441](https://github.com/ryancinsight/apollo/pull/441): the eight 256-blocks under the radix-8 pass over the parent in place, the eighths out of place, one fused four-way transpose back; under the contributor's alternating-pairs instrument on a quiet host (`../../output/apollo-base128/variant-paired/`) `f32` 2048 reads 17% under the sink on the performance core and 4 to 5.5% under on the efficiency core with every interval disjoint from zero, 4% under the scratch placement, and 5 to 7% under RustFFT in the same process; identity pairs all contain zero, no control regresses in both replications and orders (ADR 0061 note). The 2026-09-11 sink revert was the mixed-results deferral channel; the scratch placement and the instrument are removed with the reading recorded.
<a id="atlas-apollo-twiddle-unify"></a>- **ATLAS-APOLLO-TWIDDLE-UNIFY-2026-08-28** — One twiddle representation per size range [patch]. Closed; the delivery record is in git history.
<a id="atlas-apollo-resident-rows"></a>- **ATLAS-APOLLO-RESIDENT-ROWS-2026-08-28** — Register-resident row transforms [arch] (loses to batched as-built). **The shape the references have, implemented:** four-step at N = 1024 with each 32-sample row transform held entirely in registers — interleaved rows are sixteen AVX2 registers where planar needs thirty-two; DIF ordering makes every stage a whole-register pair except the last (the sample-swap-and-sign step), so no two-register sample shuffle is needed anywhere; both bit-reversals absorb into the rev-baked four-step matrix and one closing in-place involution. Five passes, zero scratch. All five oracles pass: direct DFT both directions, round trip, batched differential, decline-untouched.
<a id="atlas-apollo-n1024-specialization"></a>- **ATLAS-APOLLO-N1024-SPECIALIZATION-2026-08-31** — A per-length kernel cost 13x at the length it specialized [perf]. Closed; the delivery record is in git history.
<a id="hs-vectorize-large-kernel"></a>- **HS-VECTORIZE-LARGE-KERNEL** — vectorize outlines large kernel bodies out of the target-feature scope (hermes) [arch] (done). **Defect:** a `LaneKernel` whose `call` body is large (a fully unrolled 32-sample FFT row pass, ~500 vector ops) is not inlined into the generated `#[target_feature]` dispatcher; it compiles at baseline codegen — the dispatched symbol shows zero FMA instructions and per-operation `std_detect` re-entry — and runs ~30x slow. `#[inline(always)]` on the impl's `call` does not cure it. Small kernels inline correctly, which is why ADR 016's original evidence never hit this.
<a id="apollo-probe-4mb-variance"></a>- **APOLLO-PROBE-4MB-VARIANCE** — A valid instrument at 4 MB working sets [test]. [#449](https://github.com/ryancinsight/apollo/pull/449): the lengths from 65536 up in their own sweep and `scripts/pinned_probe.py` replicating a probe over fresh processes (median of per-run minima, ratio medians with ranges; unit-tested summary), the 262144 spread traced to per-run buffer placement rather than sampling; the chain measured at 262144 and 524288 on it — f64 262144 takes the chain at four lanes (2% under the four-step on both cores, the pattern of 131072) and f32 262144 keeps the four-step (the chain reads 16% over on the efficiency core there); 524288 keeps the four-step on both widths (the chain of four passes reads within 4% of it either way, 4% over on the efficiency core at f64). A first 524288 measurement read the chain at 0.08 of RustFFT: the plan selected the base state without an executor arm for 2^19 and the generic route ran with no twiddle table, so every chain length now carries its oracles through 524288 and that campaign is not read. (`../../output/apollo-base128/large-standing-2026-09-15/`, `large-chain-2026-09-15/`); ADR 0061 note.
<a id="apollo-moirai-local-queue-pin"></a>- **APOLLO-MOIRAI-LOCAL-QUEUE-PIN-2026-08-28** — Advance the Moirai provider revision [patch]. Delivered by PR #160, `65e6dbd0`, `fff36331`, `12e5b090`.
<a id="apollo-l1-base-two-pass"></a>- **APOLLO-L1-BASE-TWO-PASS** — A two-pass base of 256 for the L1-resident powers of two [minor] [arch] [perf]. ADR 0061 Accepted; fifteen measured slices (PRs [#401](https://github.com/ryancinsight/apollo/pull/401) to [#419](https://github.com/ryancinsight/apollo/pull/419)) took the `f64` route from 1.09 / 1.15 / 1.19 / 1.27 of RustFFT (128 / 256 / 512 / 1024) to 1.02 to 1.07 / 0.99 to 1.02 / 0.98 to 1.08 / 1.00 to 1.07 and `f32` from 1.21 / 1.32 / 1.38 / 1.17 to 1.03 to 1.10 / 1.00 to 1.04 / 1.06 to 1.14 / 0.96 to 1.05; the evidence is `../../output/apollo-base128/base256_2026-09-11.md` and the ADR's revision notes. The successors (the single-pass 512 base, its four-lane form, the aligned tables, the 128 shape, 2048) carried the route on.
<a id="apollo-base128-attribution"></a>- **APOLLO-BASE128-ATTRIBUTION** — Attribute the base-128 route at 512 and 1024 [patch] [perf]. Landed the per-phase meter on the split construction (`transform_via_base_128::<_, _, true>`, `small_sizes_against_the_references_by_core_type`; `../../output/apollo-base128/split_attribution_2026-09-10.txt`). Cycles on the performance core, `f64`: 512 = gather 205 + four blocks of (rows 235, columns with sink 311) + 365 unattributed (the stamps themselves are about 40 cycles each) = 2754 against RustFFT 2258 (1.22); 1024 = gather 942 + eight blocks of (235, 319) + 771 unattributed + one combine level 763 = 6908 against 5470 (1.29). The bare kernel (rows 235, columns 207) is 65% of the RustFFT budget at 1024 on its own; the construction adds gather 14%, sinks 13% (112 per block over the direct store, two loads, a multiply and a butterfly per chunk), level 11%.
<a id="atlas-apollo-compile-time-feature-gates"></a>- **ATLAS-APOLLO-COMPILE-TIME-FEATURE-GATES-2026-09-01** — Short kernels gate SIMD on compile-time target features [patch] [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-instance-major-64"></a>- **ATLAS-APOLLO-INSTANCE-MAJOR-64-2026-08-30** — The instance-major construction serves n = 64 [perf] [arch]. Closed; the delivery record is in git history.
<a id="apollo-n65536-four-step-scalar-loss"></a>- **APOLLO-N65536-FOUR-STEP-SCALAR-LOSS** — Remove the generic four-step loss from 65536 upward [patch] [perf]. **Outcome:** [ADR 0053](docs/adr/0053-planar-four-step-domain.md) Accepted: planar route to `2^18`; 65536 `f64` 767 to 220 µs, `f32` 219 to 108 µs; PR #351 merged 2026-09-08.
<a id="atlas-apollo-worker-retention"></a>- **ATLAS-APOLLO-WORKER-RETENTION-2026-09-03** — Per-worker scratch needs quiescent reclamation [minor] [perf] — done . **Status:** done; **commit:** `3381cd45`; **branch:** `perf/apollo-codelet-remeasure`.
<a id="atlas-apollo-eight-block-split"></a>- **ATLAS-APOLLO-EIGHT-BLOCK-SPLIT-2026-09-03** — Extend the tuned split to 1024 [minor] [perf]. **Finding.** `BASE_SPLIT_LENGTHS` is `[128, 256, 512]`. Those lengths sit at +8 to +36% against RustFFT; n = 1024, the first length past the construction, sits at **+64 to +69% in both precisions** — the largest power-of-two gap on the board. 1024 is exactly 8 x 128, so it is the next member of the family rather than a new idea.
<a id="atlas-apollo-base-64"></a>- **ATLAS-APOLLO-BASE-64-2026-08-28** — Generalize the base kernel; add the 64-point route [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-stage-fusion"></a>- **ATLAS-APOLLO-STAGE-FUSION-2026-08-26** — Carry registers across stages in the batched kernel [arch]. Closed; the delivery record is in git history.
<a id="apollo-batched-files-past-target"></a>- **APOLLO-BATCHED-FILES-PAST-TARGET** — Split the batched files past the 500-line target into leaf modules [patch]. **Outcome:** PR #375. `batched/mod.rs` (729), `radix.rs` (643), `tests.rs` (578) and `sweep.rs`'s tests split by concern into leaf modules under `batched/` and `batched/tests/`, line moves only; the fleet scan on the tree reads `oversized_files` 37 against the baseline 38 and `manifest_implementation` 23 against 24, no class up. Remaining in the baseline: `base128/tests.rs` (803).
<a id="atlas-apollo-cwt-fft-convolution"></a>- **ATLAS-APOLLO-CWT-FFT-CONVOLUTION** — Replace direct CWT evaluation with per-scale FFT convolution [minor]. Delivered by PR #187, `e2f7ed21`.
<a id="atlas-apollo-bench-regression-false-positive"></a>- **ATLAS-APOLLO-BENCH-REGRESSION-FALSE-POSITIVE-2026-09-01** — The regression gate flagged identical machine code [patch] [perf] — done 2026-09-01 (identity fixed; bias not reproduced). Delivered by PR #250, `bc141b9c`, `7196ae13`, `d3490283`.
<a id="apollo-composite-passes-generic"></a>- **APOLLO-COMPOSITE-PASSES-GENERIC** — One generic flat Stockham pass per radix in place of the AVX2 per-type clones [patch] [perf]. PR [#389](https://github.com/ryancinsight/apollo/pull/389): `radix_composite/avx2/` (2297 lines, 27 type-suffixed functions) replaced by one `LaneKernel` per radix in `radix_composite/flat_pass/` over hermes registers; `type_suffixed_fns` 44 to 17.
<a id="apollo-single-pass-512-base"></a>- **APOLLO-SINGLE-PASS-512-BASE** — A 512-point base for the eight-lane width without the gather [minor] [perf]. [#420](https://github.com/ryancinsight/apollo/pull/420): the instance-major kernel at sixteen 32-sample rows, the column pass a sixteen-point DIF in registers; `f32` 512 1.01 / 1.00 of RustFFT from 1.10 to 1.12, the efficiency core 0.97 from 1.20 (`../../output/apollo-base128/small_sizes_base512_run*_2026-09-11.txt`); ADR 0061 revised.
<a id="atlas-apollo-leto-layout-passes"></a>- **ATLAS-APOLLO-LETO-LAYOUT-PASSES-2026-08-26** — Route multidimensional layout passes through Leto [arch]. Delivered by PR #125, PR #127, `1e70b27e`, `967e6e84`.
<a id="atlas-apollo-dctdst-fast-kinds"></a>- **ATLAS-APOLLO-DCTDST-FAST-KINDS** — Implement Makhoul fast paths for DCT-I/IV and DST-I/IV [minor] (PR #197). Closed; the delivery record is in git history.
<a id="apollo-planar-sink-aligned-direct"></a>- **APOLLO-PLANAR-SINK-ALIGNED-DIRECT** — Write the sink direct where the caller's rows are on a line [patch] [perf]. **Outcome:** PR #373. With the caller's rows on a line the direct sink reads `f2` at 2048 `f32` 1.6k to 1.7k cycles against 2.4k to 2.7k staged (the transform 6.6k against 7.4k to 7.8k) and is level at the 16 KiB planes (2048 `f64`, 4096 `f32`), so the sink goes direct up to `DIRECT_SINK_MAX_PLANE_BYTES` (two pages) with the caller on a line and stages otherwise; off a line unchanged, `rustfft_comparison` 2048 level (`../../output/apollo-planar-rectangular/sinkdir_*`). The 16 KiB cells of the acceptance were not met and are recorded as level.
<a id="a"></a>- **Atlas in-house replacement roadmap — apollo slice [arch] — done** — . Apollo owns spectral transforms; it consumes leto (arrays, ndarray replacement),
<a id="apollo-4096-column-first"></a>- **APOLLO-4096-COLUMN-FIRST** — 4096 as eight 512-blocks under the radix-8 pass ahead of them [minor] [perf]. [#444](https://github.com/ryancinsight/apollo/pull/444): the plan builds the 512 state at 4096 and routes it through the eight-block column-first form at either width, no kernel change; `f64` 4096 0.74 / 0.78 of RustFFT on the performance core from 1.14 to 1.22 and `f32` 0.90 / 0.87 from 1.20 to 1.29 (20 to 24% under the four-step route), the efficiency core 0.93 to 0.97 from 1.03 to 1.15 (`../../output/apollo-base128/small_sizes_eight4096_run*_2026-09-11.txt`); ADR 0061 note. Residue: 8192 and above on the four-step route.
<a id="atlas-apollo-sht-fft-factorization"></a>- **ATLAS-APOLLO-SHT-FFT-FACTORIZATION** — Factor SHT longitude sums through FFT [minor]. Delivered by `a3cfe5cf`.
<a id="atlas-apollo-n96-column-unroll"></a>- **ATLAS-APOLLO-N96-COLUMN-UNROLL-2026-09-01** — Elide runtime column-loop control [patch] [perf]. Closed; the delivery record is in git history.
<a id="apollo-cuda-crt-linkage"></a>- **APOLLO-CUDA-CRT-LINKAGE** — Correct the native CUDA driver boundary [patch] [arch]. [PR 335](https://github.com/ryancinsight/apollo/pull/335), `dd58067c`; adopts [provider `242520e`](../hephaestus/backlog.md#heph-cuda-driver-boundary), removes eight obsolete packages and header setup. [ADR 0048](docs/adr/0048-worker-scratch-lifetime.md) records passing all-feature native tests, dependency policy and coverage limits.
<a id="atlas-apollo-small-size-next"></a>- **ATLAS-APOLLO-SMALL-SIZE-NEXT-2026-08-28** — Ranked small-size work [perf] — updated 2026-08-29 on fresh measurement. **The 2026-08-28 standing below is stale and its top-ranked increment is already delivered.** That entry read "n = 64 is the worst ratio and has no base kernel" at 1.90x. The base-64 kernel landed, and n = 64 in f64 now runs **0.735x** — Apollo faster than RustFFT, the best power-of-two ratio in the sweep. Increment (1) is closed by delivery, not by this note.
<a id="apollo-planar-split-seam-fusion"></a>- **APOLLO-PLANAR-SPLIT-SEAM-FUSION** — Fuse the odd-power decimation and combine into the stage passes [patch] [perf] — done 2026-09-09 (rejected). **Outcome:** built (a decimated source seam per half, a direct two-row combine sink on the odd half) and rejected on measurement: outputs bitwise the split's, but section totals rose 9 to 19% at 8192 and 32768 in both precisions; the source seam gains 10% in `f64` and loses up to 13% in `f32` (each half re-splits every input register), the direct combine sink loses 10 to 40% against the combine pass; evidence `../../output/apollo-planar-split-deint/` (`fusion_*`, `sections_fusion3`). Superseded by [the rectangular route](#apollo-planar-rectangular-odd-powers).
<a id="atlas-apollo-phastft-ref"></a>- **ATLAS-APOLLO-PHASTFT-REF-2026-08-25 [patch]**. Closed; the delivery record is in git history.
<a id="apollo-composite-odd-leading-stages"></a>- **APOLLO-COMPOSITE-ODD-LEADING-STAGES** — Level the orders that lead with a radix 3 [patch] [perf]. PR [#394](https://github.com/ryancinsight/apollo/pull/394): ragged rows run over into the neighbouring row with whole registers (masked only at a stage's last group), the stage loop dispatches to the register width once per transform (the per-stage `vectorize` cost about 45 cycles), and at two complexes per register the first stage packs its arms by halves, exactly. The hermes pair concatenate op was written for an exact four-wide scatter and not consumed (about 19 shuffles against the padded tile's 8 plus one store).
<a id="apollo-hermes-interleave-in-frame"></a>- **APOLLO-HERMES-INTERLEAVE-IN-FRAME** — Advance hermes past the in-frame `interleave_pairs` and re-read the four-lane chain [patch] [perf]. [#454](https://github.com/ryancinsight/apollo/pull/454): the lock at hermes 7e45dda0, the four-lane interleave frame with no call (the eight-block interleave loop at four lanes is 27 instructions a chunk (24 vector: 8 shuffles, 8 loads, 8 stores, no spill move, no call), from 86 with 40 spill moves and 4 calls; the four-block loop 16 and the three-block loop 13, likewise call-free); `f64` 131072 +1% on the performance core and +1% on the efficiency core, 32768 +7% on the performance core and +1% on the efficiency core, 262144 +1% on the performance core and +0% on the efficiency core in the phase meter; On the wall clock the advance is inside the instrument bands: the small-sizes probe (two quiet runs, `small_sizes_hermes_run{1,2}_2026-09-15.txt` against `small_sizes_sel_run{1,2}`) reads `f64` 1024 to 32768 within 2% of before on both cores (2048 at 0.95 to 0.98 of RustFFT on the performance core, 32768 at 0.98 to 1.00), and the replicated large-sizes campaign (`large-hermes-2026-09-15/` against `large-chain2-2026-09-15/`) moves `f64` 131072 on the performance core from 1.00 to 1.02 and 524288 from 1.10 to 1.06 while unchanged routes move as much (`f32` 524288 on the efficiency core -8% in apollo ns on the four-step, `f64` 262144 -4%): between campaigns the lengths past the caches swing 5 to 10% on their own, so the meter's 24 to 26% cut of the outer interleave at `f64` 16384 and 32768 (a 4% share of those transforms) is below what the probe resolves. No re-selection: the out-of-line calls were a codegen defect, not what lost 65536 and 524288 to the four-step, so those lengths keep it and the chain toggle is not re-run; the lock advance is kept for the frame it restores. (`../../output/apollo-base128/chain_phases_hermes_2026-09-15.txt`); ADR 0061 note.
<a id="atlas-apollo-aarch64-all-targets"></a>- **ATLAS-APOLLO-AARCH64-ALL-TARGETS-2026-08-27** — warning-clean non-x86 test graph [patch] (PR #156, merge `b3925141`). Closed; the delivery record is in git history.
<a id="atlas-apollo-short-prime-cvxiii"></a>- **ATLAS-APOLLO-SHORT-PRIME-CVXIII** — Re-audit f32 N=5/N=7/N=11 rows [patch]. Closed; the delivery record is in git history.
<a id="apollo-chain-pass-e-core-census"></a>- **APOLLO-CHAIN-PASS-E-CORE-CENSUS** — What the efficiency core charges the column pass for [spike]. [#453](https://github.com/ryancinsight/apollo/pull/453): the radix-8 pass loop is issue-bound (114 vector instructions a chunk at eight lanes, critical path 37 against an issue bound of 29 to 38) and Skymont executes each 256-bit instruction as two micro-ops, so the pass pays twice its issue time there at every stride (2.0 to 2.2x with the clock ratio); the four-step's batched kernel is FMA- and load-heavy and latency-bound (1.19x); the census (now `scripts/codegen_attribution.py loops`, unit-tested) also found the four-lane interleave calling hermes' `interleave_pairs` out of line (4 calls, 40 spill moves a chunk) — fixed upstream in hermes #175, [the lock advance](#apollo-hermes-interleave-in-frame); the pass's removable share is its spill traffic — [the spills item](#apollo-chain-pass-spills); ADR 0061 note.
<a id="apollo-real-2d-half-api"></a>- **APOLLO-REAL-2D-HALF-API** — Give the 2-D real transforms a half-spectrum API [minor]. [#474](https://github.com/ryancinsight/apollo/pull/474) (draft, stacked on #473): `fft_2d_array_half_into`/`ifft_2d_array_half_into` and `RealFftData::{forward,inverse}_2d_half_into` over `(nx, ny/2 + 1)`, oracles in `tests/real_half_api/plane.rs` (full-spectrum agreement, the 3-D pair on a unit-depth volume, Hermitian completion, zero allocations), and the full-spectrum 2-D entries routed through the pair where the split admits `ny` — the half staged, the plane written once in parallel row blocks; the owned forward from 4 MiB of output, where the fault spread repays its staging round trip — bit-identical to it where routed. Census (`engine_census_2d_routed_2026-09-15.txt`), widened over routed by the minimum, forward / inverse: 4096x16 1.01x / 3.10x; 4096x64 1.44x / 3.41x; 16384x16 1.26x / 3.01x; 65536x4 1.07x / 2.20x.
<a id="atlas-apollo-base-butterfly-128"></a>- **ATLAS-APOLLO-BASE-BUTTERFLY-128** — L1-resident 128-point base + 8xn chain [arch]. Delivered by PR #157, `cc94b12b`.
<a id="atlas-apollo-base128-arithmetic"></a>- **ATLAS-APOLLO-BASE128-ARITHMETIC-2026-08-28** — Close the 128-base arithmetic line [perf]. Closed; the delivery record is in git history.
<a id="apollo-2048-f32-sink"></a>- **APOLLO-2048-F32-SINK** — Close the `f32` 2048 gap under the radix-4 sink [minor] [perf]. [#439](https://github.com/ryancinsight/apollo/pull/439): 2048 at eight lanes as eight 256-blocks under a radix-8 sink with seven table twiddle rows, gathered eight-way; `f32` 2048 1.05 / 1.12 of RustFFT on the performance core from 1.19 to 1.25 (9.5% under the four-block form), the efficiency core 1.03 inside its band, `f64` on the four-block form unchanged (`../../output/apollo-base128/small_sizes_eight2048_run*_2026-09-11.txt`). Closed as a reading against parity: the census puts apollo under RustFFT in instructions and the sink phase at twenty-three L1 streams to RustFFT's nine (ADR 0061 note); successor [APOLLO-2048-COLUMN-FIRST](#apollo-2048-column-first).
<a id="atlas-apollo-four-step-twiddle-planar"></a>- **ATLAS-APOLLO-FOUR-STEP-TWIDDLE-PLANAR-2026-08-26** — Vectorize the four-step twiddle pass [patch] (by deletion). Closed; the delivery record is in git history.
<a id="atlas-apollo-accuracy-gate"></a>- **ATLAS-APOLLO-ACCURACY-GATE-2026-08-25** — Commit the analytical accuracy probe [patch] (PR #121). Closed; the delivery record is in git history.
<a id="apollo-lane-parallel-threshold"></a>- **APOLLO-LANE-PARALLEL-THRESHOLD-2026-09-11** — Derive lane parallelism from task geometry [patch] [perf]. [PR #442](https://github.com/ryancinsight/apollo/pull/442): preserve the element floor and admit five wide lane groups through the chunk-aware policy from [Moirai #328](https://github.com/ryancinsight/Moirai/pull/328); production probe and runner: ef794330.
<a id="apollo-small-length-fixed-cost"></a>- **APOLLO-SMALL-LENGTH-FIXED-COST** — Attribute the per-call cost of the lengths under 64 [patch] [perf]. [#432](https://github.com/ryancinsight/apollo/pull/432): the call path censused (`../../output/apollo-base128/base256_2026-09-11.md`), the reduced eight-point arm in 256-bit registers, and the vector frame entered once at the plan — framed executors carrying `#[target_feature]` so the register arms and the hermes eight-lane codelets inline into them. `f32` 8 1.07 of RustFFT on the performance core from 1.52 and 0.93 on the efficiency core from 1.11, three pinned runs (`../../output/apollo-base128/small_sizes_frame_run*_2026-09-11.txt`); no length under 128 slower. ADR 0061 revised.
<a id="apollo-workspace-impulse-oracle-budget"></a>- **APOLLO-WORKSPACE-IMPULSE-ORACLE-BUDGET** — Fit the workspace impulse oracle in the CI slow budget [patch]. **Outcome:** PR #382. The matrix is sharded: the lengths below 262144 keep every form and extent in one test (1.5 s here), the 262144 forms run one test each over both precisions and extents (0.8 s each here), every (length, form, extent) triple exercised as before; 3.6 s became four tests under 1.6 s.
<a id="apollo-planar-seam-prefetch"></a>- **APOLLO-PLANAR-SEAM-PREFETCH** — Prefetch the seam streams ahead of the row loop [patch] [perf] (rejected). **Outcome:** closed on a spike (`../../output/apollo-compact-fold/pf_*.txt`, sandbox only): prefetching the next tile block's sixteen source rows (256 `T0` prefetches per block) slowed the source sweep 12 to 25% at 16384 and 65536 in both precisions and left 262144 within noise; the source sweep is fill-bound from L2, not latency-bound, and the hermes prefetch operation is not filed. Re-open only with a finer-grained schedule and a measured hypothesis.
<a id="apollo-pot-chain-depth"></a>- **APOLLO-POT-CHAIN-DEPTH** — 65536 to 262144 on the column-first chain at any depth [patch] [perf]. [#448](https://github.com/ryancinsight/apollo/pull/448): the chain recursive at any depth (runtime block length, one row table per level, alternating placement), the probe measuring 65536 to 262144; selected by measurement: 131072 at four lanes on the chain (1.01 / 1.00 of RustFFT from the four-step's 1.13 on the performance core), 65536 and `f32` 131072 keep the four-step (the chain reads 1.09 / 1.08 / 1.01 / 1.25 against 0.98 / 0.94 at 65536; `f32` 131072 19% over on the efficiency core), 262144 keeps it unmeasured against the chain (instrument variance at 4 MB); residues filed below (`../../output/apollo-base128/small_sizes_deep_run*_2026-09-15.txt`, `small_sizes_sel_run*_2026-09-15.txt`); ADR 0061 note.
<a id="atlas-apollo-base-probes"></a>- **ATLAS-APOLLO-BASE-PROBES-2026-08-29** — The base kernel re-probed the CPU inside its body [perf]. Closed; the delivery record is in git history.
<a id="gpu"></a>- **GPU availability probe cleanup [major] — done** — . [x] [major] D8-gpu-availability-probe (owner Codex, completed 2026-07-16; scope `apollo-fft` GPU transport manifests/pipeline, ADR 0013, tests, and PM records): delete the hardcoded `gpu_fft_available() -> bool` probe and its re-exports. Acceptance: no Apollo-owned availability boolean or source residue, typed Hephaestus acquisition remains the only capability evidence, focused value-semantic gates and provider audit pass, and the pre-1.0 API break is documented. Default-feature checks (394 Nextest tests), source scans, and provider audit pass; the all-feature CUDA linker residual is recorded in `gap_audit.md`. `apollo-fft` advances to 0.23.0.
<a id="d13-"></a>- **D13-refresh-provider-lock [patch] — done** — . Owner: Codex; scope: workspace `Cargo.lock` provider revisions and Apollo PM records.
<a id="atlas-apollo-plan-length-safety"></a>- **ATLAS-APOLLO-PLAN-LENGTH-SAFETY-2026-08-27** — Validate slice length at FftPlan1D entry [patch]. Delivered by PR #155.
<a id="atlas-apollo-f32-n96-codelet"></a>- **ATLAS-APOLLO-F32-N96-CODELET-2026-08-31** — Reduce the N = 96 fixed-codelet latency gap [patch] [perf]. Delivered by PR #225, PR #111, `074dedee`, `eea29fd3`.
<a id="apollo-n8-f64-gap"></a>- **APOLLO-N8-F64-GAP-2026-09-04** — Superseded for the plan by [the framed arm](#apollo-n8-f64-framed), 2026-09-17. N=8 carries the same unexamined "scalar by measurement" note [minor] [perf] (note confirmed). **Result: the note was right, its reasoning was not.** The AVX arm is built, passes the direct-DFT oracle in both directions and an impulse at every one of the eight positions, and loses. Two vector forms were measured; the arm is not wired, and stays test-gated as the subject of the instrument that declined it. | arm | P-core | E-core | | --- | --- | --- | | scalar Winograd (shipped) | **11.33 ns** | **15.81 ns** | | AVX, generic `avx_cmul_precise` twiddles | 17.24 ns | 24.44 ns | | AVX, folded `(direct, swapped)` coefficients | 15.08 ns | 21.58 ns | Forward plus normalized inverse, one round trip, intervals disjoint over three runs. Folding the twiddles bought 13% and 15% and did not change the verdict, which is the useful part: the better form still loses by a third.
<a id="apollo-codelet-experiment-boundary"></a>- **APOLLO-CODELET-EXPERIMENT-BOUNDARY** — Keep rejected codelet routes in tests [patch]. `7013ea96` removes unused production selection and confines split phases to tests, net -71 lines against incoming main. [Gates](../../output/apollo-square-transpose/integration/merge-gates/collection.json) pass 126 debug/126 release cases; [code evidence](../../output/apollo-square-transpose/integration/merge-gates/census/pe-code-identity.json) preserves instructions and a 512-byte size margin, without a timing claim.
<a id="apollo-four-step-e-core-f32-131072"></a>- **APOLLO-FOUR-STEP-E-CORE-F32-131072** — Attribute the four-step's efficiency-core advantage at `f32` 131072 [spike]. [#450](https://github.com/ryancinsight/apollo/pull/450): the chain's per-level phases on both cores (`chain_phases_by_core_type`, the `MEASURE` variant of `column_first::chain`): every column pass costs 2.0 to 2.1x its performance-core cycles on the efficiency core at every level, the 2 KB-stride level included, so the aliasing hypothesis is falsified and the mechanism is the passes' shuffle-and-FMA throughput (the chain 1.69x slower on that core, the four-step 1.19x, the clock ratio 1.24); follow-up [dup-split rows](#apollo-chain-dup-split-rows) (`../../output/apollo-base128/chain_phases_2026-09-15.txt`); ADR 0061 note.
<a id="apollo-real-static-split-bound"></a>- **APOLLO-REAL-STATIC-SPLIT-BOUND** — Let the static-length real transforms take the split [minor]. [#478](https://github.com/ryancinsight/apollo/pull/478): the static forms take the split through the plan-free runtime kernel ([ADR 0063](docs/adr/0063-static-real-split-through-the-runtime-kernel.md) over the four bound options), no bound or signature change so the class falls to [minor]; the inverse wherever the split admits `N`, the forward from 128 or at any non-power-of-two length (the static plan's constant-length kernels win below: 0.44x at 4 and 8, 0.86x at 32), measured in `static_split_probe_2026-09-15.txt`: forward 1.02-1.36x from 128, inverse 1.01-1.54x at every length; `tests/real_half_api/static_forms.rs` pins agreement with the dynamic split within the bound, zero warm allocations, and which route ran (the split forward's bit-exact conjugate upper half, the split inverse's untouched scratch) for f64, f32 and F16.
<a id="d15-"></a>- **D15-align-hephaestus-legacy-math-pin [patch] — done** — . Owner: Codex `/root`; scope: `Cargo.lock` Hephaestus package revisions and synchronized Apollo PM records. No Apollo source or manifest compatibility path changes.
<a id="atlas-apollo-workspace-rustdoc-runtime"></a>- **ATLAS-APOLLO-WORKSPACE-RUSTDOC-RUNTIME-2026-08-26** — Bound the workspace documentation gate [patch]. Landed as dca1139d (option 1): a `plan` job partitions the member list from `cargo metadata` at runtime into four shards and a matrix job documents each with its own doc directory, every crate covered; the four `rustdoc (...)` jobs and `rustdoc shards` run green on every PR (verified 2026-09-15 on #459).
<a id="✅ atlas-apollo-be"></a>- **ATLAS-APOLLO-BENCH-BASELINE-LOCKFILE-2026-09-01** — Benchmark baseline leg cannot build a PR that adds a dependency [patch]. Historical unlocked candidate-lock transplantation is superseded by [revision-owned locked baseline preparation](#apollo-benchmark-baseline-closure). Its dependency-added fixture established lock inconsistency, not compatibility of historical production source with candidate provider APIs.
<a id="apollo-small-pot-arms-probe-guard"></a>- **APOLLO-SMALL-POT-ARMS-PROBE-GUARD** — Guard the `small_pot_arms` direct arms with the frame probe [patch]. [#462](https://github.com/ryancinsight/apollo/pull/462): `vector_frame_available()` gates the vector-direct, vector-fused and lanes-framed arms in `assert_arms_agree` and `arms_for_size`, each arm debug-asserts the frame, and the SAFETY comments name the probe and callers that discharge them; the probe ran unchanged on this host under bench-quick, both core types, every arm reported.
<a id="apollo-chain-dup-split-rows"></a>- **APOLLO-CHAIN-DUP-SPLIT-ROWS** — Dup-split twiddle rows for the column pass [patch] [perf]. [#452](https://github.com/ryancinsight/apollo/pull/452): falsified and not retained — the doubled twiddle stream costs the outer levels 21 to 79% a pass on both cores while the L1-resident innermost level gains 7 to 15%; `f32` 131072 -11% on the performance core, +1% on the efficiency core (`../../output/apollo-base128/chain_phases_dup_split_2026-09-15.txt`); the innermost pass keeps its 2x efficiency-core ratio with a third fewer shuffles, so the follow-up is [the pass census](#apollo-chain-pass-e-core-census); ADR 0061 note.
<a id="atlas-apollo-composite-radix-wrong-answers"></a>- **ATLAS-APOLLO-COMPOSITE-RADIX-WRONG-ANSWERS-2026-08-28** — 109 FFT lengths return wrong results or panic [major]. Closed; the delivery record is in git history.
<a id="apollo-4096-as-eight-512-blocks"></a>- **APOLLO-4096-AS-EIGHT-512-BLOCKS** — Run 4096 as eight sixteen-row blocks under a radix-8 step [minor] [perf]. Rejected on the pinned probe, two runs (`../../output/apollo-base128/small_sizes_4096_run*_2026-09-11.txt`, the route as `../../output/apollo-base128/eight_blocks_4096_route_2026-09-11.patch`): `f32` 4096 4 to 7% over the four-step route on the performance core and 7% over on the efficiency core, `f64` 5 to 9% under on the performance core and 10% over on the efficiency core — the working set (the parent and seven scratch blocks, 60 to 120 KB) past L1 at both scalars. Recorded on ADR 0061; the four-step route stays, and a base route at 4096 needs an L2-aware shape.
<a id="apollo-fft-cross-thread-plan-retention"></a><a id="cross-thread-plan-retention"></a>- **APOLLO-FFT-CROSS-THREAD-PLAN-RETENTION-2026-09-02** — Kernel plan caches kept one copy per thread [patch] [perf]. **Delivered.** `cached_plan`, `cached_four_step_planes`, and `cached_resident_plan` now sit behind a process-wide map, following the two-level pattern the twiddle caches and `orchestration::cache::plans` already use. The thread-local map stays the lock-free fast path; a miss takes the shared table instead of building a private one.
<a id="atlas-apollo-batched-parallel"></a>- **ATLAS-APOLLO-BATCHED-PARALLEL-2026-08-25** — Parallelize the batched stage set [patch]. **Outcome:** rejected; disjoint strided-column Moirai units regressed every measured size, while widening the sequential route made the unpinned production census topology-sensitive and slower. The existing `N >= 65536` parallel generic route remains unchanged.
<a id="d16-"></a>- **D16-native-benchmark-regression-oracle [minor] [arch] — done** — . Owner: Codex `/root`; scope: `apollo-bench` statistical report contract, native comparison boundary, benchmark CI, ADR 0036, and synchronized PM records. Transform kernels and provider implementations are non-goals.
<a id="d8-fft-"></a>- **D8-FFT-dispatch-verification-tree [arch] — done** — . Delivered by PR #46.
<a id="apollo-half-volume-fallback-review"></a>- **APOLLO-HALF-VOLUME-FALLBACK-REVIEW** — Post-merge review of the half-volume fallback scratch reuse [patch]. Reviewed [#456](https://github.com/ryancinsight/apollo/pull/456) at d034108b: `lanes::paired` derives each group's input offset from the task index exactly as moirai's `for_each_chunk_mut_enumerated_with` indexes its chunks (`start = c * chunk_size`), the ragged last group is bounded by `group_lanes`, both slices are asserted whole-lane at entry, and the 2-D staging role is borrowed only inside the paired closure before `xy_axes_inplace` runs, where no 1-D lane reaches it. Findings: none.
<a id="apollo-python-release-crlf"></a>- **APOLLO-PYTHON-RELEASE-CRLF-2026-09-06** — Normalize the Python release workflow [patch]. `df5447d0` repairs the CRLF blob introduced by `0917cacd`; merged-index inspection reports `i/lf w/lf` for all five workflows under the committed `*.yml text eol=lf` policy. No fresh-clone check is claimed.
<a id="apollo-transpose-isa-contract"></a>- **APOLLO-TRANSPOSE-ISA-CONTRACT** — Preserve AVX transpose preconditions [patch]. [PR 337](https://github.com/ryancinsight/apollo/pull/337), `9da1f9f7`; AVX-contract correction passes native/analytical/allocation/size gates and [landed CI](https://github.com/ryancinsight/apollo/actions/runs/34015129487). Census binary -512 bytes; unchanged comparison supports neither gain nor regression.
<a id="apollo-sdft-recurrence-stability"></a>- **APOLLO-SDFT-RECURRENCE-STABILITY** — Bound sliding-DFT rounding drift [patch]. [#461](https://github.com/ryancinsight/apollo/pull/461): the modulated form (one N-entry table, the leaving and entering samples on the same entry, the accumulator never multiplied) with a round-robin refresh from the window every max(1, N/K) updates; `SdftPlan::drift_bound` derives u·A·(N² + 4N + max(K, N)(N + 6)), count-independent. Measured at N = 48, K = 6 over a million f64 updates against the direct DFT: the unit-circle recurrence 1.1e-12 to 2.8e-12 against the 8.3e-13 bound (oracle included), the new form 5e-15 to 8e-15 at every checkpoint. The crate computes in f64 only, so the bound is stated for one precision.
<a id="apollo-1024-as-two-512-blocks"></a>- **APOLLO-1024-AS-TWO-512-BLOCKS** — Measure 1024 as two sixteen-row blocks under a combining sink [patch] [perf]. Rejected on the pinned probe, two runs (`../../output/apollo-base128/small_sizes_two512_run*_2026-09-11.txt`, the route as `../../output/apollo-base128/two512_route_2026-09-11.patch`): `f64` 1024 1.10 / 1.12 of RustFFT from 1.03 to 1.05 on four 256-blocks, `f32` 1.12 / 1.09 from 0.99 to 1.02; the phase meter read the two-block route 5% under the four-block one and the wall clock decided. Recorded on ADR 0061; the four-block route stays, the two-block step stays deleted.
<a id="apollo-pot-radix-chain"></a>- **APOLLO-POT-RADIX-CHAIN** — 8192 to 32768 as column-first radix chains over the 256 and 512 bases [patch] [perf]. [#447](https://github.com/ryancinsight/apollo/pull/447): the column-first step nested (in place outside, out of place inside through a slice-length temporary), a radix-4 arm and four-block interleave, 8192 and 16384 over the 256 base and 32768 over the 512 base at either width, the probe measuring 8192 and 16384; apollo / RustFFT `f64` 8192 0.98 / 0.97 on the performance core from 1.05 (+13% in apollo ns) and 1.00 / 0.99 on the efficiency core from 0.99 (-0%); `f32` 8192 0.97 / 0.91 on the performance core from 1.19 (+24% in apollo ns) and 1.00 / 1.00 on the efficiency core from 1.05 (+4%); `f64` 16384 0.95 / 0.91 on the performance core from 1.06 (+12% in apollo ns) and 1.00 / 0.99 on the efficiency core from 0.96 (-4%); `f32` 16384 0.95 / 0.96 on the performance core from 1.05 (+13% in apollo ns) and 1.01 / 1.01 on the efficiency core from 0.99 (-2%); `f64` 32768 0.90 / 0.97 on the performance core from 1.07 (+10% in apollo ns) and 0.96 / 0.95 on the efficiency core from 0.95 (-1%); `f32` 32768 0.91 / 0.96 on the performance core from 1.05 (+13% in apollo ns) and 0.95 / 0.94 on the efficiency core from 0.90 (-4%) (`../../output/apollo-base128/small_sizes_chain_run*_2026-09-15.txt` against `small_sizes_pot_standing_run1_2026-09-15.txt`); ADR 0061 note.
<a id="d11-"></a>- **D11-remove-policy-wrapper [major] — done** — . Owner: Codex; scope: `apollo-fft` radix-composite execution policy module, package version metadata, ADR 0035, and synchronized PM records.
<a id="apollo-ci-workspace-job-killed"></a>- **APOLLO-CI-WORKSPACE-JOB-KILLED** — The workspace test job dies in a runner shutdown at its parallel test compile [patch] [ci]. The "rust workspace" job of `ci.yml` ended in "the runner has received a shutdown signal" (exit 143) on five consecutive runs from 2026-09-11 21:26Z (main run 34649073993 after #439, PRs #440, #441, #442 and their reruns on 2026-09-14), each about 3.5 minutes in while cargo compiled the members' test binaries in parallel under the default full debuginfo; the hosted `ubuntu-24.04` runner has 16 GB and its runner service is what an out-of-memory VM stops. Fix: the debuginfo discipline the stack's siblings already declare (moirai's form) — line tables only for the workspace, none for dependencies, build scripts and proc-macros, in `dev` and `test` — which cuts the compile's resident memory and the test binaries' size, backtraces keeping their lines. Gate: `cargo nextest run --locked -p apollo-fft` and the doctests under the new profiles in a main-based sandbox; the job's next green run on main closes the retro-verification gap. Re-open trigger: a further exit-143 at the same step, whose next step is `CARGO_BUILD_JOBS` on the test compile.
<a id="apollo-codelets-over-lanes"></a>- **APOLLO-CODELETS-OVER-LANES** — Decide the short codelets instantiated over lane vectors as the composite route's base [minor] [arch]. [ADR 0062](docs/adr/0062-composite-bases-as-register-kernels.md) (Accepted; [#435](https://github.com/ryancinsight/apollo/pull/435)): codelets over lane vectors rejected — the generator's `WinogradScalar` bound is a real-field contract no lane type meets, 180 uses five of eight lanes, and the generated body stages through a scratch — for register kernels under column passes; 180 measured as five register-resident 36-point transforms under a radix-5 column pass at `f32`: 0.94 / 1.03 of RustFFT on the performance core from 1.43 to 1.47, 0.98 / 0.98 on the efficiency core from 1.25 (`../../output/apollo-base128/small_sizes_column180_run{3,4}_2026-09-11.txt`), on hermes [#174](https://github.com/ryancinsight/hermes/pull/174).
<a id="apollo-miri-schedule-split-coverage"></a>- **APOLLO-MIRI-SCHEDULE-SPLIT-COVERAGE** — Put the split schedule's scratch under a UB checker [patch]. Clean under both aliasing models on a quiet host: `schedule_controls_preserve_composite_values` drives `Fused` and `Split` through dft50 and dft144 in both directions and reports **no undefined behaviour** — stacked borrows 4.6 s, `-Zmiri-tree-borrows` 10.5 s. ADR 0051's scratch-initialization argument now has checker evidence, not only the structural argument and its token-pinned test.
<a id="atlas-apollo-core-class-labels"></a>- **ATLAS-APOLLO-CORE-CLASS-LABELS-2026-09-01** — Pinned probes select cores by queried class [patch]. Closed; the delivery record is in git history.
<a id="apollo-sft-sublinear-recovery"></a>- **APOLLO-SFT-SUBLINEAR-RECOVERY** — Deliver a sublinear sparse recovery route [minor]. [#479](https://github.com/ryancinsight/apollo/pull/479): `SparseFftPlan::downsampled` — Hsieh, Lu, Pei downsampling with syndrome decoding up to four tones a bucket, the count doubling on a failed round so recovery is exact for any exactly K-sparse input and O(K log K) while four-way collisions suffice; `RecoveryRoute` on the configuration, the dense top-K route the oracle ([ADR 0064](docs/adr/0064-sparse-recovery-by-downsampling.md)). Random K-sparse supports recovered with the dense route's support and values within the derived bound; `benches/recovery.rs` at K = 16: 8-12 µs at every length 4096 to 1048576 against the dense route's 122 µs to 33 ms (`sft_recovery_2026-09-15.txt`).
<a id="d18-"></a>- **D18-close-leto-boundary-and-fft-stack [patch] — done** — . Owner: Codex `/root`; scope: current Leto/Hephaestus lock convergence, removal of the retired `ndarray-compat` boundary, and Apollo FFT bounded flat-cache initialization. FFT algorithms, cache capacity, and benchmark workloads are non-goals.
<a id="apollo-n1m-planar-crossover"></a>- **APOLLO-N1M-PLANAR-CROSSOVER** — Decide the planar route at 1048576 [patch] [perf]. **Outcome:** `PLANAR_MAX_LEN` moved to `2^20` ([ADR 0053](docs/adr/0053-planar-four-step-domain.md) revised): six-run quiet-host census, `f64` 1048576 10.0 to 5.7 ms (RustFFT 6.4 to 7.1, PhastFT 6.9 to 7.5), `f32` 5.1 to 6.0 to 2.7 (PhastFT 3.1 to 3.4); 2097152 halves, level in `f64`, 10% behind PhastFT in `f32`; the PhastFT comparison pinned; evidence `../../output/apollo-n1m-crossover/`; PR #362 merged 2026-09-09.
<a id="apollo-worker-hook-admission"></a>- **APOLLO-WORKER-HOOK-ADMISSION** — Preserve reclamation under bounded registration [patch] [arch]. [#464](https://github.com/ryancinsight/apollo/pull/464): Moirai 9a0af648 bounds admission (MAX_IDLE_HOOKS slots, `register_idle_hook` refusing with CapacityExhausted, snapshot-then-run); Apollo records a refused registration once per process instead of panicking in its infallible plan constructors, transforms run either way, `thread_local_scratch_hook_registered` reports it, and the hook lives in `kernel::scratch_hook` with a registry-filling test. The moirai direct pin is gone from the manifest (git+version source); the lock advance to f89c9acf rides #460.
<a id="atlas-apollo-permute-fold"></a>- **ATLAS-APOLLO-PERMUTE-FOLD-2026-08-28** — Remove the planar route's repair pass [perf]. Closed; the delivery record is in git history.
<a id="atlas-apollo-retained-footprint"></a>- **ATLAS-APOLLO-RETAINED-FOOTPRINT-2026-08-27** — Attribute and reduce the retained working set [perf]. Delivered by PR #169, PR #187, `f4ef9708`.
